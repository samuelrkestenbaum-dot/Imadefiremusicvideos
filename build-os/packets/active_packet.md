# Active Packet

> The one packet currently in flight. The orchestrator reads this every session;
> the builder implements exactly this and nothing else; the archivist clears it
> on close. One packet at a time.

- **Status:** **NONE ACTIVE.** P-027 (PAPER PACKET — ROADMAP re-time +
  APPARITION_STANDARD + waterfront plant) **CLOSED 2026-07-03**, main-loop
  close, receipt `build-os/receipts/P-027.md`, commit 77d72dd. Docs only, no
  generation.

## Staged next — first real back-half media packet

The paper groundwork is done: `when-it-rains/ROADMAP.md` now carries the
verified section clock, `when-it-rains/APPARITION_STANDARD.md` is the law for
every "her" sighting, and the waterfront plant is threaded V3/V4 → PRE2 →
BRIDGE.

**Build order (correct times now in ROADMAP):**
1. **CH1 extension (89.8/102 → 132.7, ~30s)** — the locked chorus_v16 covers
   only the first ~12s of a 42.9s chorus. Build the hero-clip-as-motif +
   1–2 fresh cafe-reflection beats so the film is continuous past the lock AND
   the chorus stops "starving" (director note). Do with/before V3/V4.
2. **V3/V4 (132.7–183.4)** — home again, searching: closet (empty hangers +
   ONE dress), drawer (her things + the WATERFRONT PHOTO plant), the turned
   hallway mirror → turns it back, only him (rung 5, audience-only). Grade the
   mirror beat against APPARITION_STANDARD.

**Every sighting from here grades against APPARITION_STANDARD.md before ship.**

## Hard rules in force (standing)

- **NO bare chest, ever**; salvage wardrobe via nano/local edit, never reroll.
- Likeness law: user gates picks vs `review/ANCHOR_BOARD.png`; max 2 nano
  passes on face frames. **Element-refit with wir-him (`<<<1b581c11...>>>`) is
  the fix for AI-look faces** — same scene, face pulled to canonical; lock pose
  explicitly (nano reimagines composition ~half the time); check BROWN eyes.
- **Render pipeline is 1080p** (all scripts) — never downscale high-res takes.
  **Topaz (not bytedance)** for real detail on <1080p takes.
- Frame-exact stitch law (tpad+trim). July Reverb mix (same clock, verified).
- wan: roles start_image/audio_references exact; behavioral-pin the performance;
  breath gaps in the vocal are correct sync, not defects.
- Delivery: phone-encode (crf24 ~37MB for 90s 1080p); >50MB chat sends fail
  silently — verify file_uuid.
- **No external mutation without go**; standing method board→gate→build→QC→
  receipt.

## Read first for a new session
`when-it-rains/HANDOFF.md` (QUALITY PASS addendum FIRST, then P-025/P-024),
`DIRECTORS_NOTES.md`, `ROADMAP.md` (re-timed), `APPARITION_STANDARD.md`,
`PRODUCTION_PLAYBOOK.md`.
