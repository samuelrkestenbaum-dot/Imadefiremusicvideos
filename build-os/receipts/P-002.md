# Receipt — P-002: Render-Review Checklist (timecode-keyed) for the "When It Rains" rough cut

- **Date:** 2026-06-29
- **Authority:** marketing-media.
- **Route:** builder → reviewer → archivist (plus an independent qa arithmetic
  pass run by the main loop).
- **Branch base (merge-base):** `a20ba80` on
  `claude/when-it-rains-music-video-fetr0z` (repo has no trunk; "green" is judged
  against the branch tip).

## Scope
- **In:** One new doc — `when-it-rains/RENDER_REVIEW.md` (+209 lines) — a
  playback-ordered, timecode-keyed checklist the user runs while watching the
  rendered rough cut. Per-cut PASS/FAIL rows for all 76 EDL cuts with computed
  running in→out timecodes, a section-time confirmation block for the ambiguous
  mid-song splits (V2/PRE1/CH1, V4/PRE2), a Targeted watch-points section, and a
  next-step routing footer.
- **Out (explicit):** ZERO product/pipeline edits. No EDL, clips/stills CSVs,
  scripts, assets, or analysis touched. No `build-os/` or `.claude/` edits by the
  builder. No Higgsfield generation/upscale (credits = external mutation). No
  push/merge/deploy.

## Commits
- `464924d` Add RENDER_REVIEW.md — 76-cut timecode checklist for rough-cut review
  (76-cut timecode checklist core; green in isolation).
- `1f38024` Add targeted watch-points + next-step routing to RENDER_REVIEW.md
  (additive: timecoded watch-points + HANDOFF next-step routing footer).
- Combined diff `a20ba80..1f38024`: a single changed path
  (`when-it-rains/RENDER_REVIEW.md`), 132 + 77 = 209 insertions.

## QA proof — GREEN (8/8 checks)
- **Timecodes:** all 76 running timecodes recomputed independently and match;
  EDL duration sum = **276.0s exactly**, **zero mismatches** against the doc.
- **11-clip derivation:** the regenerated set
  (C01, C04, C06, C07, C13, C15, C16, C19, C22, C23, C24) was independently
  re-derived from `data/clips.csv` vs `data/clips_original_backup.csv` by job_id
  diff — the diff equals **exactly** the doc's set (no more, no fewer).
- **C02 overuse:** the 11× / 37s C02 placements in the doc match REVIEW §1.
- **FINAL truncation:** the ~2s FINAL truncation (276.0s EDL vs 274s mix target)
  matches `FOOTAGE_AUDIT.md` §6.
- **Scope:** the change is the single new file `when-it-rains/RENDER_REVIEW.md`.
- **Commit-1 isolation:** `464924d` stands green on its own (self-contained doc).
- **Working tree:** clean (both before and after the close commit).
- **Safety grep:** clean — no secrets, no live code, benign doc text only.
- (No software test suite exists; this is a marketing/media doc deliverable. The
  doc is itself the instrument for the still-pending human-eye render proof.)

## Review — PASS
- **Verdict:** PASS. No fixes required.
- Source-accurate: every claim traces to source (EDL/REVIEW/FOOTAGE_AUDIT); no
  unsupported claim.
- Genuinely usable live: playback-ordered, terse PASS/FAIL rows, watch-points
  placed after the linear pass so they don't interrupt the first watch.
- Watch-points are specific and timecoded; the output footer routes results into
  HANDOFF open threads #2 (beat-lock) and #4.
- **Codex second-eyes: UNAVAILABLE** — single-reviewer pass. Recorded as a caveat.
- Two minor, non-blocking notes:
  1. C07 framing — the doc's post-regen "does the reflection now appear" check is
     correct as written.
  2. Render depends on the gitignored `song.mp3`; `REVIEW.md` Step 0 already
     covers re-attaching it, so this is informational, not a defect.

## Residue
- `when-it-rains/RENDER_REVIEW.md` now exists as the structured capture
  instrument for the human render review — but the review itself is still
  pending. The gap (no human-eye confirmation of the 11 regenerated clips / the
  full cut) remains open until the user renders on their Mac and marks the doc up.
- Carried-forward deferred packets (see `build-os/memory/residue.md`): beat-lock
  the EDL + trim ~2s off FINAL (blocked on the user's mid-song section times +
  MP3 re-attach); band-coverage batch (~8 clips) to reduce C02's 11× overuse
  (media authority, after the lead is locked); selective 2K/4K upscale (last).
- Known risks unchanged: regenerated clips verified by automated re-analysis, not
  human eye; C04 head-turn still absent; lead face varies slightly clip-to-clip;
  song MP3 not in git; no render possible in the cloud env.

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` — local
  commits only (this closing receipt commit included) without explicit go.
- **No Higgsfield generation / upscale** — credits = external mutation; STOP
  unless inside a confirmed media packet with go.
- **Next real work is off-machine** — the user renders + reviews the rough cut on
  their Mac using `when-it-rains/RENDER_REVIEW.md`; no cloud-buildable packet is
  in flight until that marked-up review returns.
