# Receipt — P-025: PRE1 realism repair (element-refit of the delivered faces)

**Status:** CLOSED — repaired `pre1_v1.mp4` + `first90.mp4` delivered, final
frame-QC verdict **SHIP** (2026-07-03)
**Written by:** archivist.

## Scope

- **In:** User-directed repair of the delivered P-024 PRE1. Trigger: the user
  rejected the PRE1 faces as **"extremely like AI"**. Repair rule: **the CUT
  SHAPE STAYS** — same scenes, same choreography, same timings; only the
  face/realism is pulled to the canonical likeness. Media packet under
  `when-it-rains/` (nano element-refits, re-animation, 2K, CI-relay re-render,
  delivery — Higgsfield spends inside the packet go).
- **Out (explicit):** LOCKED intro_v6 / chorus_v16 / hero `491e39d1`
  (untouched); everything past 89.8; any merge / deploy / publish / secrets;
  the training-photo history purge (still user-gated, unanswered); the P-026
  paper packet (ROADMAP re-time) — staged, not started.

## What shipped

- **Repaired `pre1_v1.mp4`** — the PRE1 section cut with both face zones
  refit to canonical.
- **Repaired `first90.mp4`** (89.79s) — continuous 0:00–89.8, frame-exact
  grid, July Reverb bed, with the repaired PRE1 swapped in. Final frame-QC
  = **SHIP**; the pass explicitly answered the packet's own question —
  *"would the AI-look complainer be satisfied? **Yes**."*
- **Phone encode delivered** with the chat `file_uuid` **confirmed**
  (delivery-size law held; full-quality master stays in the repo).
- Branch: **`claude/when-it-rains-handoff-l0u075`** (no trunk; green judged
  against the branch tip; CI relay branch-agnostic).

## The method (validated this packet): nano ELEMENT-REFIT

- **Element-refit is THE fix for AI-look faces:** nano edit of the existing
  scene master with element **wir-him** (`1b581c11`) embedded via the
  `<<<uuid>>>` placeholder — element **BOUND** (verified via
  `reference_elements` in the job echo). Same scene, face pulled to canonical.
- **Measured realism deltas (QC'd against the originals at delivery res):**
  turn **4.5 → 9/10**, walk **5 → 8/10**.
- **User gated the picks:** **WC** (walk) + **TA** (turn), per the likeness
  law (vs `review/ANCHOR_BOARD.png`).
- **Walk chain paint-out, v2:** the WC refit needed a local chain paint-out.
  **v1 FAILED QC as an orange line** (original chroma kept at lifted luma);
  **v2 passed** — one-sided **luminance lift + row-local skin-chroma
  adoption**. New law for paint-outs on DARK chains.
- **Failures found (now QC checklist items):**
  - One nano refit **failed server-side** (`66db1de3`) — retried, no pattern.
  - **Eye-color drift in 2/5 refits** (brown → grey-green) — **check brown
    eyes explicitly** in every future refit QC.
  - Expect ~1/6 refits to add a chain (soul gold-chain bias persists through
    refits).

## Final assets

- **Walk:** `walk_final.png` = WC refit `d19e7262` + local chain paint-out v2
  → imported `d6142bc8` → kling take **`d035bb3b`** (`sound:"off"` explicit;
  slot in-points **remeasured**: A in **0.00** / B in **5.50**).
- **Turn:** TA refit `557974ad` → imported `c7a5e1c1` → wan sync
  **`7db11194`** — the P-024 **pinned-behavior brief reused verbatim, passed
  first try** → 2K **`e15b36e1`**.
- **Realism survives the pipeline:** the refit realism held through kling/wan
  animation and the 2K + grain + compression chain — but verify at delivery
  res in the final pass anyway (this packet did).

## Proof (frame-QC via vision subagent — sandbox eyes, law 0b)

- Refit candidate batches (3 walk + 2 turn) frame-QC'd **against the
  originals with explicit realism deltas** before the user gate; paint-out v1
  caught and rejected in QC (orange line); both re-takes frame-QC'd raw
  before 2K; repaired first90 queued for a full final pass.
- **Final pass = SHIP**, including the packet's acceptance question ("would
  the AI-look complainer be satisfied? Yes").
- No software test suite (media packet) — exact counts are the QC passes
  above. Safety: no secrets committed; `song.mp3` (July Reverb mix) stays
  gitignored; **no bare chest** (standing hard rule held); max-2-nano-passes
  likeness law held (one refit pass per master + local deterministic
  paint-out, zero additional face passes).

## Reviewer verdict

- **SHIP** — the final frame-QC pass on the repaired first90 is the shipping
  judgment (accepted process deviation for media packets, precedent
  P-018/P-020/P-022/P-023/P-024; no formal qa/reviewer agents; Codex
  second-eyes not run — not applicable to the CI-relay media workflow).

## Commits

- **Base `414b235`** (P-024 close + delivery-size law; branch in sync with
  origin at open). Range **`6409a53` → `e68afe3`** — **12 commits (8 authored
  relay + 4 CI render commits)**, plus this receipt/close commit on top.
  Working tree clean at close, **0 ahead / 0 behind origin** (CI relay is the
  established push mechanism, not a new mutation).
  Key points: `6409a53` (refit candidates queued), `69e98fb`/`509db05` (walk
  master final; paint-out v2 after v1 failed QC), `c45b85c` (re-takes queued,
  superseded candidates purged), `81b7f6d` (refit walk take + remeasured
  in-points into `render_pre1.sh`), `dc38978` (refit turn sync 2K swapped;
  repaired PRE1 + first90 render triggered), `8c11c8c` (final-eyes queue),
  `e68afe3` (QC purge + HANDOFF P-025 addendum).
- **≤2-commit contract: acknowledged inapplicable** to the CI-relay media
  workflow (same recorded precedent as P-020..P-024).

## Credits

- **1371.75 at open → 1371.75 at last check.** The packet's actual spend is
  **~25–35** (est. 40–60), which appears to sit **within earlier balance
  reads** — the pre- and post-packet balance snapshots read **identical**,
  which is odd. Possibly enhanced-account credit accounting; **recorded as an
  oddity, not overclaimed** — treat 1371.75 as the best-known balance and
  re-read at the next spend.

## Residue (carried UNCHANGED from P-024 — nothing new deferred)

- **Training photos remain in PUBLIC git history** — purge offered, still
  unanswered (carried since P-018/P-019). Needs explicit go (history rewrite).
- **3 bare-chest curb rolls still in the Higgsfield library**
  (`733152dc` / `8c9b80ef` / `85cb2d58`) — user to delete in the Higgsfield
  UI (still unconfirmed).
- **Locked-intro jewelry flag:** thin chain + ring ~19–21s inside the
  user-LOCKED intro_v6 — decide **before FINAL assembly**. Record only.
- **ROADMAP back-half clock still VERIFIED WRONG** — do NOT generate against
  it; the re-time is the staged **P-026 paper packet** (below).
- New QC learnings (eye-color drift check, chain-paint-out chroma law,
  ~1/6 refit chain bias) are canonized in the HANDOFF P-025 addendum — laws,
  not open items.

## Open boundaries

- No merge / deploy / publish / secrets without explicit go (CI auto-push of
  render commits on this branch is the established relay mechanism).
- Training-photo history purge: user-gated, unanswered.
- Locked material stays locked: hero `491e39d1`, chorus_v16, intro_v6.
- New Higgsfield generation only inside a media packet with go — **P-026 as
  staged is a PAPER packet (zero generation)**.

## Next

**PAPER PACKET P-026 — re-time ROADMAP to the verified section clock +
APPARITION LEGIBILITY STANDARD + waterfront-photo plant** (per
`when-it-rains/DIRECTORS_NOTES.md` must-fix #1/#2 + STRUCTURAL; verified
clock table in its addendum — CH1 really 89.8–132.7, FINAL only 16.2s).
Renumbered from the old "P-025" paper staging when this repair packet took
the id. Zero generation; required before any back-half generation.
