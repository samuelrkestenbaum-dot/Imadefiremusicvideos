# Receipt — P-005

- **Packet id:** P-005
- **Title:** Band-coverage ADD-CUTS plan for dragging PRE2 + CH1 (spec only, no generation)
- **Date:** 2026-06-29
- **Authority:** marketing-media (planning artifact only — no generation, no product-file mutation).
- **Route:** builder → reviewer → qa (independent numeric + fidelity) → archivist.
- **Branch / base:** `claude/when-it-rains-music-video-fetr0z`; base `5d61a89` (P-004 close). HEAD after close `bbd952a`.

## Scope

**In:** a single new planning artifact, `when-it-rains/BAND_COVERAGE_PLAN.md`, specifying **10 new band-coverage cuts** (C25–C30 for PRE2, C31–C34 for CH1). Each cut is fully specified: clip-id, band-only shot, reference-anchored `nano_banana_2` still recipe, `kling3_0/std/off/5s` clip recipe, draft prompt, EDL slot, and target duration. Plus a 3-line additive `REVIEW.md` §7 pointer cross-referencing the held band-coverage idea list to this concrete spec.

**Explicitly OUT:** no generation (no Higgsfield spend), no edit to `data/edl.csv` / `clips.csv` / `stills.csv` / `assets.json`, no re-timing, no render. Generation is the separate **gated** P-006; edl insert + re-time is the separate **gated** P-007.

## Commits

- `cff6043` — Add BAND_COVERAGE_PLAN.md — staged add-cuts spec for PRE2/CH1 pacing (`when-it-rains/BAND_COVERAGE_PLAN.md`, +128 lines, new file).
- `bbd952a` — REVIEW.md §7: point to BAND_COVERAGE_PLAN.md (`when-it-rains/REVIEW.md`, +3 lines).

Both authored on a docs/plan-only basis; no product file touched.

## QA proof — GREEN (10/10)

- **Pacing math (independently re-derived):**
  - PRE2 span **41.00s**, +6 cuts → 12 cuts, **3.417s** avg (6→12).
  - CH1 span **38.75s**, +4 cuts → 12 cuts, **3.229s** avg (8→12).
- **Recipe-UUID fidelity:** all 4 recipe UUIDs char-for-char correct vs HANDOFF / FOOTAGE_AUDIT — selfie IDs, anchor `5cc8239a`, preset `24bae836…`.
- **Clip-id collision:** C25–C34 — no collision with existing clip-ids.
- **declined_preset_id:** ON for the 6 PRE2 cuts / OFF for the 4 CH1 cuts (as specified).
- **Completeness:** all 10 cuts fully specified (shot, still recipe, clip recipe, draft prompt, EDL slot, target duration).
- **Data unchanged:** data blobs (`edl` / `clips` / `stills` / `assets`) byte-unchanged.
- **No generation:** confirmed — zero Higgsfield calls, zero credit spend.
- **Commit-1 isolation:** clean (commit `cff6043` stands green on its own).
- **Safety grep:** clean.

## Reviewer — PASS

Editorial coherence verified: PRE2 = storm-build / CH1 = warm-release; cuts are **band-world-only** → no likeness risk. Placements target the documented long holds (after C17 / 8.2s and C12 / 6.2s), **interleaved not appended**. The bald-fix (reference-anchoring + on-model line) is baked into the recipes. Gating is honest (generation + edit are correctly deferred to P-006 / P-007).

- **Codex second-eyes:** UNAVAILABLE this session (single-reviewer PASS — caveat noted).
- **Non-blocking downstream notes (carry to generation):**
  1. At generation, ensure **C26 (full-band push-in)** reads visually distinct from existing **C09 (also a push-in)**.
  2. The plan's relative `data/edl.csv` path is correct **from `when-it-rains/`**; whoever runs P-007 **from repo root** should use `when-it-rains/data/edl.csv`.

## Scope guarantee

The two close commits and this packet touched **only**: `when-it-rains/BAND_COVERAGE_PLAN.md` (new) and `when-it-rains/REVIEW.md` (+3 lines). No `data/*` blob, no script, no render, no external mutation, no credit spend.

## Residue / open boundaries

- **P-006 (GATED — credits / external mutation):** generate the 10 staged clips (C25–C34) on Higgsfield. Needs explicit go.
- **P-007 (GATED — edit):** insert the 10 clips into `data/edl.csv` + re-time PRE2 / CH1 to ~3.3s avg. Needs explicit go.
- The PRE2 / CH1 drag risk (sections stretched ×1.55 / ×1.64 in P-004) now has a **concrete remediation path staged** in `when-it-rains/BAND_COVERAGE_PLAN.md`.
- 2 non-blocking generation-time notes recorded above (C26-vs-C09 distinctness; relative-path caveat).
- No push / merge / deploy — local commits only on `claude/when-it-rains-music-video-fetr0z`.
