# Receipt — P-011: Browser-playable EDL preview tool (`preview.html` + `build_preview.py`)

- **Date:** 2026-06-29
- **Branch base (merge-base):** `46adfc0` (on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip)
- **Authority:** build (read-only media-data input → HTML artifact; no generation, no source-CSV change). Route: builder → reviewer → qa → archivist.

## Scope
- **In:** A deterministic generator `when-it-rains/scripts/build_preview.py` that reads `data/edl.csv` + `data/clips.csv`, joins `clip_key → mp4_url`, and emits a **self-contained** `when-it-rains/preview.html` — a browser-playable review tool that plays all **86** EDL cuts in order, streaming the clip mp4s from the Higgsfield CDN (browser-reachable; this cloud session's egress is blocked). Per-cut overlay (n/86, section, cumulative timecode, clip, note); **C25–C34** band-coverage highlighted with **C27/C33** badged on-model; per-cut PASS/FAIL + note review marking (localStorage persistence + copy/export mirroring `RENDER_REVIEW.md`); play/pause/prev/next + click-to-jump cut list; graceful per-clip failure (degrade, not hang); a clear SILENT / approximate-timing banner pointing at the real Mac render path. The generator carries an embedded self-test (every EDL clip_key resolves, exactly 86 cuts, total == 274.0, monotonic cumulative starts, no external resource refs in the emitted HTML; non-zero exit on any breach).
- **Out (explicit):** No Higgsfield generation / upscale (credits = external mutation). No change to any source CSV (`edl.csv`, `clips.csv`, `stills.csv`) — they are read-only inputs here. No change to `assets.json`. No refresh of the stale `RENDER_REVIEW.md` (the static checklist — see Residue; flagged but explicitly deferred). No push / merge / deploy.

## Commits
- `645df31` Add build_preview.py — deterministic browser EDL preview generator — `when-it-rains/scripts/build_preview.py`, **700 insertions**, base `46adfc0`. Reads `data/edl.csv` + `data/clips.csv`, joins `clip_key → mp4_url`, emits the self-contained `preview.html`; embedded self-test + non-zero exit on breach; deterministic (same inputs → byte-identical output); mirrors the HERE/DATA anchoring + self-test pattern of `resync_edl.py` / `insert_band_coverage.py`.
- `a4266c5` Add preview.html — browser-playable 86-cut rough-cut review — `when-it-rains/preview.html`, **1498 insertions**. The self-contained generated artifact: `<video>` playlist of the 86 cuts in EDL order (sets src on change, seeks to in_point on loadedmetadata, advances after each cut's duration, degrades gracefully on a failed clip); overlay + C25–C34 highlight + C27/C33 on-model badge; click-to-jump list; per-cut PASS/FAIL + note (localStorage + export); silent/approximate banner naming `fetch_assets.sh` → `assemble_rough_cut.sh` as the real render. No external `<script src>` / `<link>` / `@import` / CDN libs.

## QA proof — GREEN
- **Suite:** no software test suite (deliverable is a rendered video; the play-test is the user's). QA = generator + artifact correctness checks → **10/10 passed**.
  1. EDL order: `preview.html` plays exactly the **86** cuts in `edl.csv` order.
  2. `mp4_url`s: every cut's stream URL + note is **verbatim** from `clips.csv` / `edl.csv`.
  3. Timecodes: cumulative cut starts are **monotonic**, ending at **274.0**.
  4. Counts: **86** cuts (matches `edl.csv`).
  5. New-clip flags: **C25–C34** band coverage correctly highlighted.
  6. On-model flags: **C27 / C33** correctly badged on-model.
  7. Self-contained: **no** external `<script src>` / `<link>` / `@import` in the emitted HTML.
  8. Determinism: byte-identical re-runs (same inputs → identical output).
  9. Commit-1 isolation: `build_preview.py` (`645df31`) regenerates the `preview.html` artifact **identically** on its own.
  10. Safety grep: clean (no secrets / no stray edits).
- **Commit-1 iso:** GREEN — `645df31` (the generator) builds and self-tests green on its own and regenerates the artifact byte-identically.
- **Safety grep:** none found.
- **UI smoke:** the `preview.html` playback engine was traced statically — play/pause/prev/next + click-to-jump are wired; failed clips degrade (skip/advance) and do not hang; no stall or double-advance path. The live in-browser play-test is the **user's** (the Higgsfield CDN is egress-blocked in this cloud session, so the clips cannot be streamed/eyeballed here).

## Review — PASS
- **Verdict:** PASS.
- **Findings:** Generator is deterministic (same inputs → byte-identical `preview.html`). Playback engine has no stall or double-advance bug — all paths traced; failed clips degrade rather than hang. The artifact is self-contained (no external libs) with an honest silent/approximate banner, and the overlay/note rendering is XSS-safe. Scope is airtight: only `when-it-rains/scripts/build_preview.py` + `when-it-rains/preview.html` (both read-only-input → artifact); no source CSV, no generation, no external mutation.
- **Codex second-eyes:** **UNAVAILABLE** — single-reviewer pass.
- **Reviewer-flagged (out of P-011 scope):** `when-it-rains/RENDER_REVIEW.md` is **STALE** — recorded as a new residue finding (see Residue / `residue.md`). `preview.html` supersedes the static checklist for the live review; a refresh is an optional follow-up.

## Residue
- **`when-it-rains/RENDER_REVIEW.md` is STALE (NEW finding, reviewer-flagged, out of P-011 scope).** Its per-cut checklist still describes the **pre-band-coverage 76-cut / 276.0s** edit — predating the P-004 section-sync (274s) and the P-006/P-007 band coverage (86 cuts). `data/edl.csv` and the new `preview.html` are **correct at 86 / 274.0**; `preview.html` supersedes the static checklist for the live review. A refresh of `RENDER_REVIEW.md` to 86 / 274 (plus updated watch-points: band coverage added, on-model confirmed, section times confirmed) is an **OPTIONAL follow-up pending the user's decision**. Logged in `build-os/memory/residue.md`.
- **The render-review is still the open user step.** The full 86-cut / 274.0s edit has **not been watched by a human** — `preview.html` now offers an in-browser (silent, approximate-timing) review path, and the full Mac render (`scripts/fetch_assets.sh` → `scripts/assemble_rough_cut.sh`) remains the high-fidelity path. Either way, the human-eye review is the user's.
- Prior residue threads remain as-is (P-010 stills backfill — optional, needs go; sub-beat beat-grid quantization — deferred; fast V3 cuts — verify on render; selective 2K/4K upscale — last).

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` (repo has no trunk) — local commits only.
- **No further Higgsfield generation / upscale** — credits = external mutation; needs a fresh go inside a media packet.
- An optional `RENDER_REVIEW.md` refresh (to 86 / 274) is **not** authorized under P-011 — it needs the user's go.

---
_Closed by the archivist 2026-06-29. P-011 — browser-playable EDL preview tool (`preview.html` + `build_preview.py`); qa GREEN (10/10 + Commit-1 isolation), reviewer PASS (Codex unavailable — single-reviewer; the live play-test is the user's). Commits `645df31` + `a4266c5`, base `46adfc0`. Local commits only — not pushed._
