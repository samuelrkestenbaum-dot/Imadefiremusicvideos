# Receipt — P-015: Beat-lock analysis + gated beat-aware EDL variant

- **Date:** 2026-07-01
- **Branch base (merge-base):** `9ba310b` (on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip). Chain: `9ba310b` ← `c65748e` ← `9370af3` (HEAD).
- **Authority:** build (in-session audio analysis + a NON-DESTRUCTIVE derived data artifact under `when-it-rains/`). Route: builder → qa → reviewer → archivist.

## Scope
- **In:** Add an in-session beat-analysis capability and produce a **non-destructive** beat-aware EDL variant.
  - **Decode** `when-it-rains/song.mp3` via the bundled `imageio_ffmpeg` and build a **uniform beat grid** at the `song.json` period (**61.5234375 BPM / 0.9752381 s**), **phase-locked to onset-envelope energy** (method `librosa_onset_phase_grid`). librosa 0.11.0's raw `~184.6 BPM` (a 3× subdivision) is kept only as **labeled corroboration**, NOT used for snapping.
  - Write the durable map `when-it-rains/analysis/beats.json` (survives the gitignored `song.mp3` disappearing), the diagnostic `when-it-rains/analysis/beat_vs_cut.md` over the live 86-cut edit, and the **gated** variant `when-it-rains/data/edl_beatlocked.csv`.
  - Ship the tool `when-it-rains/scripts/beat_lock.py` + a 20-check suite `when-it-rains/scripts/test_beat_lock.py`.
- **Out (explicit):** No mutation of the live edit or any product/render surface — `data/edl.csv`, `clips.csv`, `stills.csv`, `assets.json`, `scripts/preflight_edl.py`, `scripts/render_master.sh` are all **untouched** (verified empty diff). Promoting `edl_beatlocked.csv` into the live edit is a **user-gated** decision (not done). No downbeat-based snapping (confidence too low — see caveats). No Higgsfield generation / credits. `song.mp3` NOT committed (gitignored). No push / merge / deploy.

## Method (in-session, reproducible while song.mp3 present)
- **Decode:** `when-it-rains/song.mp3` decoded via the **bundled `imageio_ffmpeg`** (no system ffmpeg required).
- **Grid:** a uniform beat grid at the `song.json` period (`61.5234375` BPM = `0.9752381` s/beat), **phase-locked** to the onset-envelope energy peak — method label `librosa_onset_phase_grid`. This snaps cuts to **beats**, not downbeats.
- **Corroboration only:** librosa 0.11.0's raw estimate (`~184.6 BPM`, a 3× subdivision of the true tempo) is recorded in `beats.json` as a labeled cross-check and is **NOT** used to place any beat.
- **Durability:** `analysis/beats.json` is the durable artifact — it survives the gitignored `song.mp3` disappearing in a future session. Re-running the suite from a bare checkout requires `song.mp3` re-attached in `when-it-rains/` (by design — see caveats).

## Commits
- `c65748e` **P-015 Commit 1: beat-lock analysis + beat/downbeat map (green in isolation)** — `when-it-rains/scripts/beat_lock.py` (562), `when-it-rains/scripts/test_beat_lock.py` (137), `when-it-rains/analysis/beats.json` (374), `when-it-rains/analysis/beat_vs_cut.md` (108). **4 files, 1181 insertions.** Base `9ba310b`.
- `9370af3` **P-015 Commit 2: gated beat-aware EDL variant data/edl_beatlocked.csv** — `when-it-rains/data/edl_beatlocked.csv` (87). **1 file, 87 insertions.** Base `c65748e` (tip).
- Two commits (≤2 per packet). Across both: **5 files, 1268 insertions, 0 modifications**. Commit-1 stands green in isolation.

## QA proof — GREEN (8/8, incl. 4 core gates)
- **Suite:** no product test suite (deliverable is a rendered video); QA = the `test_beat_lock.py` 20-check suite + the non-destructive / integrity / determinism gates below. **8/8 QA checks passed; the tool suite is 20/20.**
- **NON-DESTRUCTIVE INVARIANT — HELD:** `git diff 9ba310b..9370af3` is **EMPTY** for `data/edl.csv`, `clips.csv`, `stills.csv`, `assets.json`, `scripts/preflight_edl.py`, `scripts/render_master.sh`. `--stat` = **5 files, 1268 insertions, 0 modifications**.
- **Variant integrity (`data/edl_beatlocked.csv`):** **86 rows + header** (schema matches `edl.csv`); **11 distinct sections**, all **START times identical to live** (INTRO 0.0, V1 18.0, V2 41.25, PRE1 69.75, CH1 89.25, V3 128.0, V4 144.5, PRE2 173.0, CH2 214.0, BRIDGE 238.0, FINAL 261.0); **total 274.0 s**; **0 over-reads** (`in_point+dur > 5.0`); **0 unresolved clip_keys**; **0 back-to-back identical adjacency**. **84 / 86 cuts snapped** (real work).
- **Determinism:** `beats.json` md5 `5fcf02866956d60c4bcb321f3a3cecb4`, `edl_beatlocked.csv` md5 `876b38a640cfb787d0c647b7fd8f7bc1`, `beat_vs_cut.md` md5 `1a8e06484da640688de9ea1874175efd` — identical across **2 regens** and **== committed**.
- **Commit-1 green in isolation:** verified via a **throwaway git worktree @ `c65748e`** (with `song.mp3` present): variant absent; `--analyze-only` regenerates `beats.json` **byte-identically**; `edl.csv` byte-identical; suite **20/20**.
- **Full suite at tip:** **20 passed / 0 failed.** `preflight_edl.py` (live `edl.csv`) still **RESULT: PASS, exit 0, 0 critical**.
- **Safety grep — clean:** `subprocess` = bundled ffmpeg decode + self-invocation only; `os.remove` = the variant file only; no secrets / network / destructive-product ops. `song.mp3` NOT committed (gitignored); `beats.json` IS tracked.
- **UI smoke:** n/a (no UI artifact). Load-bearing surfaces — the empty-diff invariant, the 86-row variant integrity, and determinism — re-verified against live data (above).

## Alignment gain (informational)
- Cuts within **±50 ms of a beat: 8.1 % (live) → 84.9 % (variant)**. Mean `|offset|` **256.0 ms → 52.7 ms**; median **250.5 ms → 3.0 ms**. This is the potential gain **if** the variant is later promoted — it is not applied to the live edit.

## Review — PASS
- **Verdict:** PASS (reviewer, this session).
- Non-destructive invariant confirmed (empty diff on all 6 protected surfaces); variant integrity independently checked (86 rows, section starts preserved, 274.0 s, 0 over-reads / 0 unresolved / 0 bad adjacency); determinism confirmed (md5s reproduce and match committed); Commit-1 isolation confirmed in a throwaway worktree; the method's honest framing (beats not downbeats; librosa raw as corroboration only) is accurate; scope airtight (analysis + one gated data artifact, nothing product-facing mutated).
- **Codex second-eyes:** UNAVAILABLE this session — **reviewer ran SOLO** (single-reviewer).

## Residue / caveats (recorded to residue.md)
- **Downbeat-phase confidence LOW** — best 4/4 phase ≈ **720 ms** mean section-start error → the variant snaps to **BEATS, not downbeats**; downbeat times in `beats.json` are **advisory only**.
- **One residual ~836 ms offset** is the **final cut end** pinned by the `274.0` total + preserve-section-start constraints (intentional, conservative).
- **Commit-1 isolation is ENVIRONMENT-dependent** — a bare checkout of `c65748e` WITHOUT the gitignored `song.mp3` cannot regenerate (`FileNotFoundError`), **by design** (`beats.json` is the durable artifact). Anyone re-running the suite needs `song.mp3` in `when-it-rains/`. **Not a defect.**
- **Promoting `edl_beatlocked.csv` into the live edit** is deferred-by-design — a **user-gated** decision after human review.
- **2K/4K upscale** stays deferred (post render-approval; needs footage + credits).
- An in-session **song-synced animatic** (placeholder visuals, real timings) was delivered to the user as the pacing proof — a **scratchpad artifact, not committed**.

## Standing state / open threads
- **Render pipeline commits — now recorded (were unreceipted before now).** These landed this session and are part of the shipped render pipeline (NOT their own packet, but recorded in memory):
  - `82ac71e` — `when-it-rains/scripts/render_master.sh`: one-shot `validate → fetch → assemble → verify` wrapper (131 ins).
  - `9ba310b` — `when-it-rains/scripts/preflight_edl.py`: offline render-readiness validator, + `render_master.sh` gated on it (113 ins across 2 files). This commit is also P-015's base.
- **The 86-cut / 274.0 s live edit** (`data/edl.csv`) is **validated render-ready** (`preflight_edl.py` 0-critical).
- **BLOCKED / user-side:** the REAL footage master **cannot render in-session** — the Higgsfield CDN (`d8j0ntlcm91z4.cloudfront.net`) is **egress-blocked (403, org policy)**. Unblock requires the user to allowlist the host for the session OR run `scripts/render_master.sh` on a connected machine. This is a **policy boundary, not routable**.

## Open boundaries (awaiting explicit go)
- **NOT YET PUSHED:** commits `c65748e`, `9370af3` (P-015) and the archivist's own `build-os/` close commit are **local-only**, awaiting the user's explicit push go.
- **No push / merge / deploy** on `claude/when-it-rains-music-video-fetr0z` without explicit go.
- **No Higgsfield generation / upscale / credits** without a fresh media-packet go.
- **Promoting the beat-locked variant** into the live edit is user-gated.

---
_Closed by the archivist 2026-07-01. P-015 added an in-session beat-analysis capability (`beat_lock.py` + 20-check suite) and a NON-DESTRUCTIVE, gated beat-aware EDL variant (`data/edl_beatlocked.csv`, 84/86 cuts snapped, ±50 ms alignment 8.1 % → 84.9 %), leaving the live 86-cut / 274.0 s edit byte-untouched (empty diff on all 6 protected surfaces). qa GREEN 8/8 (suite 20/20, Commit-1 iso verified in a throwaway worktree, determinism by md5, safety clean); reviewer PASS (Codex unavailable — solo). Base `9ba310b`; tip `9370af3`. The render pipeline commits `82ac71e` + `9ba310b` are recorded here as shipped (unreceipted before now). Live edit is preflight-validated render-ready; the real render is blocked in-session on the egress-blocked Higgsfield CDN (user must allowlist or render on a connected Mac). P-015 commits + this close are local-only — awaiting an explicit push go._
