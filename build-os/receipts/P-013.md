# Receipt — P-013: Doc-coherence fix from system audit (README.md + EDIT_MAP.md to 86-cut / 274s state)

- **Date:** 2026-06-29
- **Branch base (merge-base):** `1f5211f` (on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip). Verified: `git rev-parse 8981252^` == `1f5211f`, `git rev-parse 0c88c54^` == `8981252`, so the chain is `1f5211f` ← `8981252` ← `0c88c54` (HEAD).
- **Authority:** marketing-media (docs-consistency sub-scope on `when-it-rains/README.md` + `when-it-rains/EDIT_MAP.md`; **no generation, no source-CSV / functional change**). Route: builder → reviewer → qa → archivist.

## Scope
- **In:** Reconcile the two stale project docs to the live **86-cut / 274.0s / 4:34** edit, fixing the doc-coherence findings surfaced by the system audit (AUDIT-001 — D4 EDIT_MAP staleness; D5-M1 / D5-M2 README schema + runtime drift).
  - **`README.md`** (audit D5-M1 / D5-M2): runtime **4:36 → 4:34 / 274**; **clips.csv schema** column order fixed (corrected to the actual `mp4_url` / `section` ordering); **stills.csv schema** fixed (removed phantom `beat` / `on_model` columns that the doc claimed but the real CSV header does not have); cut-overhang figure **2s → 0.25s**.
  - **`EDIT_MAP.md`** (audit D4): runtime → **274 / 4:34**; an **authority banner** added (states `data/edl.csv` is the authoritative cut list, the doc is descriptive); **11 confirmed section starts** recorded; the **C25–C34** band-coverage registry added; **EX2 marked unused**.
- **Out (explicit):** No change to any source CSV (`edl.csv`, `clips.csv`, `stills.csv`) or to `assets.json` — read-only inputs here, used only to verify the docs byte-for-byte. **The stills-catalog / `source_still` backfill was deliberately NOT done** (declined-P-010 territory; needs a fresh go + the 10 regen UUIDs from Higgsfield). No Higgsfield generation / upscale. No re-timing / re-cutting of the edit. No push / merge / deploy.

## Commits
- `8981252` Fix README.md data-schema + runtime drift (audit D5-M1/M2, 4:34/86) — `when-it-rains/README.md`, **8 insertions / 8 deletions** (one file), base `1f5211f`.
- `0c88c54` Refresh EDIT_MAP.md to 86-cut/274s + C25-C34 registry + authority banner (audit D4) — `when-it-rains/EDIT_MAP.md`, **50 insertions / 16 deletions** (one file), base `8981252`.
- Two commits, one file each (≤2 per packet). Commit-1 (`8981252`, README) stands green in isolation.

## QA proof — GREEN (9/9)
- **Suite:** no software test suite (the deliverable is a rendered video; the human render-review is the user's). QA = doc-consistency correctness checks against the live `data/` CSVs and `edl.csv`. **9/9 passed.**
  1. README runtime: reads **86 / 274 / 4:34** (no stale 4:36 / 276 current-claims).
  2. EDIT_MAP runtime: reads **274 / 4:34**.
  3. clips.csv schema: the documented column order matches the **actual `data/clips.csv` header byte-for-byte** (`mp4_url` / `section` corrected).
  4. stills.csv schema: the documented columns match the **actual `data/stills.csv` header byte-for-byte** (phantom `beat` / `on_model` removed).
  5. Overhang: corrected **2s → 0.25s**.
  6. Section starts: the **11 confirmed section starts** in EDIT_MAP verified against `data/edl.csv`.
  7. C25–C34 registry + EX2-unused present in EDIT_MAP.
  8. Narrative / arc preserved (no creative rewrite — descriptive coherence only).
  9. Commit-1 isolation + safety grep.
- **Commit-1 iso:** GREEN — `8981252` (README) is self-contained and stands green on its own (before the EDIT_MAP commit).
- **Safety grep:** clean — only `when-it-rains/README.md` and `when-it-rains/EDIT_MAP.md` touched; no secrets, no stray edits, nothing outside the two docs.
- **UI smoke:** n/a (no UI artifact in this packet; both surfaces are static docs). The load-bearing surfaces — runtime figures, the two CSV schema lines, the section starts — were independently re-verified against the live data (qa items 1–6).

## Review — PASS
- **Verdict:** PASS.
- **Findings:** Every number was verified against live data — the runtime figures, the two CSV schema lines (checked byte-for-byte against the actual `data/clips.csv` / `data/stills.csv` headers), the 11 section starts (vs `data/edl.csv`), and the C25–C34 registry. No overreach: the builder correctly **declined to do the stills backfill** (that is declined-P-010 territory and would touch a source catalog + needs the 10 Higgsfield regen UUIDs). Scope is clean — only the two docs, doc-only, no functional / CSV change, no external mutation.
- **Codex second-eyes:** **UNAVAILABLE** — single-reviewer pass.

## Residue
- **D4 (EDIT_MAP doc-staleness) → RESOLVED (P-013).** EDIT_MAP now matches the live 86-cut / 274.0s edit, carries the authority banner, the 11 confirmed section starts, the C25–C34 registry, and EX2-marked-unused.
- **D5-M1 / D5-M2 (README schema + runtime drift) → RESOLVED (P-013).** README runtime → 86 / 274 / 4:34; clips.csv + stills.csv schema lines now match the real CSV headers byte-for-byte; overhang corrected 2s → 0.25s.
- **Stills-catalog backfill NOT done (declined-P-010 territory).** The audit re-quantified it (AUDIT-001 D5-M3 / M4): **13 / 36 clips' `source_still` don't resolve to `stills.csv`** — driven by **10 reference-anchored regen stills generated on Higgsfield but never written into `stills.csv` / `assets.json`** (+ EX1/EX2 "prior" + C04 `regenA1` placeholder); plus **4 superseded off-model stills** (`22e02845` / `b0f3fd47` / `26443c5f` / `ff3797c3`) still in the catalog unflagged. **All NON-FUNCTIONAL** (no script reads `source_still` or the stills catalog for assembly). GATED — needs the 10 regen UUIDs from Higgsfield; **user DECLINED**.
- **The render-review is still the open user step.** The full 86-cut / 274.0s edit has not been watched by a human; both review instruments (`preview.html` silent browser pass + `RENDER_REVIEW.md` audio-render checklist) are current at 86/274.
- Prior residue threads remain as-is, all user / optional: sub-beat beat-grid quantization (deferred, post-approval); selective 2K/4K upscale (last, post-approval).

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` (repo has no trunk) — local commits only.
- **No further Higgsfield generation / upscale** — credits = external mutation; needs a fresh go inside a media packet (this is also what the declined stills backfill would require).
- **Post-approval polish is premature** — sub-beat quantization + upscale are explicitly later, after the user approves the cut.

---
_Closed by the archivist 2026-06-29. P-013 — doc-coherence fix from the system audit: `README.md` + `EDIT_MAP.md` reconciled to the live 86-cut / 274.0s / 4:34 state (audit D4, D5-M1, D5-M2); qa GREEN (9/9 + Commit-1 isolation), reviewer PASS (Codex unavailable — single-reviewer). Commits `8981252` (README) + `0c88c54` (EDIT_MAP), base `1f5211f`. Both project docs are now coherent with the 86/274 edit. The stills backfill was deliberately not done (declined-P-010). Local commits only — not pushed._
