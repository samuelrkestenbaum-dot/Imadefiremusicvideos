# Receipt — P-020: Verse 1, the kitchen (song 23.5–43.0s)

**Status:** CLOSED — USER LOCKED v1_v1 with "Great" (2026-07-02)
**Written by:** main loop (deviation: archivist subagent unavailable — usage
credits exhausted at close time; receipt format preserved).

## What shipped
- `v1_v1.mp4` (19.5s): two-mugs beat on "soft touch upon my skin" (one take,
  frame-measured cuts) → sung line "my heart had crossed the ocean" at the
  kitchen window (wan2_7, his vocal, 2K aigc) → rain-rivers insert ("where
  the rivers bend", ffmpeg punch-in) → the mug goes back, door closes →
  rain outro. Delivered and locked.
- `first43.mp4`: continuous 0:00–0:43 stitch (intro_v6 + v1_v1 over one
  unbroken audio slice) as a checkpoint deliverable.

## Locked asset IDs
See `when-it-rains/V1_SCENE_SPEC.md` (LOCKED header) — master chain ends at
`4709db96`; mug take `a61bf1b4`; sync chain `f0d7f1c7 → 7005ac53 → wan2_7
405a0f7d → 2K f9fd14a7`; rain insert `4f58453e → aaea0be1`; audio
`v1_bed.mp3`, `v1_line3.mp3` (media `3824741b`), `first43.mp3`.

## Defects caught by the review loop (and fixes)
1. **Wrong-person sync (CRITICAL):** first wan2_7 call used generic roles
   `image`/`audio`; inputs silently failed to bind (medias echoed empty) and
   wan invented a different person. Survived to the first cut because the raw
   wan output skipped the frame-QC gate. Caught in final-cut QC BEFORE user
   delivery; fixed with exact roles `start_image`/`audio_references`
   (models_explore), binding verified in the job echo, raw output frame-QC'd,
   re-upscaled, re-rendered; bad cut deleted (commit a760b9d). Playbook
   gained the ROLES LAW (commit 1fa2fa8). Wasted spend: 1× wan2_7, 1× 2K.
2. **Stale review evidence:** fetch_review.sh skips existing files; the old
   committed `review/v1_v1.mp4` masked the re-render during QC. Fixed by
   purging same-named review copies on re-queue (commit 1ca12a6).
3. Master iteration (all pre-user): gaze-at-camera regen; gold-chain removal
   (Soul keeps generating it — likely learned from training photos); photo
   neutralized then removed entirely (user: "not sure the value"); charcoal
   tee; full text scrub (kettle/bottle/book spines) after user reminder.

## Capability unlocked
`pip install imageio-ffmpeg` → static ffmpeg in the sandbox. Audio slicing is
now local; the song is never committed to the public repo.

## Next
P-021: V2 board (43.0–70.3) — first exterior: leaving the flat, the street in
rain, look-over-shoulder beat, "I need my world to stop" hold. Then PRE1, CH1,
V3/V4, PRE2, CH2, BRIDGE, FINAL.

## Residue (carried)
- Soul chain-necklace bias: check EVERY new soul_2 still of him.
- P-017 driver code may still carry the wrong wan role names (unverified).
- Training photos remain in public git history (purge offered, unanswered).
- Credits estimate after this packet: roughly 1500–1600 of 2415.
- Archivist/subagent usage credits exhausted — receipts may need main-loop
  fallback until resolved.
