# Receipt — P-016: Director's cut — late-90s / 2000s aesthetic re-edit

- **Date:** 2026-07-01 (receipted 2026-07-01 — this receipt clears a prior receipt debt; the P-016 commit landed before P-017 but was never receipted).
- **Branch base (merge-base):** the P-016 commit is `60b297d` on `claude/when-it-rains-music-video-fetr0z` (no trunk — judged against branch tip). Preceded by `0320218` (treatment) ← `3b4bc4d` (verify-fix).
- **Authority:** marketing-media (a full creative re-edit — new Higgsfield clips + a deterministic re-sequence of `when-it-rains/data/`, per explicit user direction to replace the 70s rough cut with a late-90s / early-2000s rain-ballad look). Route: builder → qa → reviewer → archivist.

## Scope
- **In:** Replace the 70s-styled rough cut with a **late-90s / early-2000s rain-ballad** director's cut whose **lyrics + beat grid drive the cuts**:
  - **24 new reference-anchored Higgsfield clips** — the artist as a **buzz-cut, fit, soaked 2000s frontman** + a **consistent memory woman** + atmosphere, all in a **bleach-bypass desaturated** look (**~198 credits**). Recorded in `data/clips.csv` (+ `stills.csv`).
  - `scripts/build_edit_v2.py` — deterministically sequences the 24 shots to the lyrics + beat grid (`analysis/beats.json`) into `data/edl.csv`: cuts land on beats, **no clip repeats within an 8-cut window** (fixes the old C02-×11 recycling), in-points shift on reuse. Result: **107 cuts, 274.00 s**.
  - **Old 70s edit preserved** as `data/{edl,clips,stills}.70s.csv` (non-destructive — history kept, not deleted).
  - `fetch_assets.sh` — skip stills rows with no URL (the new stills are generation-only).
  - `TREATMENT.md` — the director's treatment re-skinned to the 2000s aesthetic.
  - **Frontman likeness corrected across 3 anchor iterations** (buzz cut + fit build; canonical still `00d037d0`).
- **Out (explicit):** The 70s edit is preserved, not discarded. No push / merge / deploy. `song.mp3` NOT committed (gitignored).

## Commits
- `60b297d` **P-016: director's cut — late-90s / 2000s lyric+beat-driven re-edit** — `TREATMENT.md` (re-skin), `data/clips.csv` / `data/stills.csv` (24 new clips + stills), `data/edl.csv` (107-cut re-sequence), `data/clips.70s.csv` / `data/edl.70s.csv` / `data/stills.70s.csv` (old 70s edit preserved), `scripts/build_edit_v2.py` (109, the deterministic sequencer), `scripts/fetch_assets.sh` (+1, skip URL-less stills rows). **9 files, +485 / −196.**
- Preceding this session (context, not P-016's own commits): `3b4bc4d` (fix `render_master` verify: `has_stream` false-negative under `pipefail`) and `0320218` (director's treatment v1 for reaction).

## QA proof — PASS (preflight-verified)
- **Suite:** no software test suite (deliverable is a rendered video); proof = the preflight validator over the new edit.
- **`preflight_edl.py` on the new edit: RESULT PASS, 0 critical, 0 warnings** — **107 cuts, 274.00 s, 24 clips, every cut resolves, no over-reads.**
- **No clip repeats within an 8-cut window** (verified by `build_edit_v2.py`'s sequencing constraint — fixes the old C02-×11 recycling).
- **Non-destructive to history:** the old 70s edit is preserved verbatim as `data/{edl,clips,stills}.70s.csv`.
- **Safety:** `song.mp3` NOT committed (gitignored).

## Review — PASS (with a flagged caveat)
- **Verdict:** PASS.
- The 24 clips are reference-anchored to a consistent frontman + memory woman; the sequence is deterministic and preflight-clean at 107 / 274.00; the old edit is preserved; the treatment matches the delivered aesthetic.
- **Verify pass was via Higgsfield `video_analysis`** — **8 character clips read on-model**; **a few perf clips read "shaved" rather than "buzzed"** (flagged for a possible re-roll). **AI lip-sync is not frame-accurate** (a known limitation of the generated performance shots — the motivation for P-017).
- **Codex second-eyes:** not recorded for this packet.

## Residue / caveats (recorded to residue.md)
- **"Bald vs buzz" on some perf clips** — a few of the 24 read "shaved" rather than "buzzed"; flagged for a possible re-roll. Frontman likeness was corrected across 3 anchor iterations (canonical still `00d037d0`).
- **AI lip-sync is not frame-accurate** on the generated performance shots — the reason P-017 built the wan2_7 lip-sync pipeline (itself now unproven — see residue).
- **~198 credits spent** on the 24-clip generation (external mutation, was authorized by the user's re-edit direction).

## Open boundaries (awaiting explicit go)
- **NOT YET PUSHED:** `60b297d` (P-016) + `3b4bc4d` + `0320218` are **local-only**, awaiting the user's explicit push go.
- **No push / merge / deploy** without explicit go. Any **re-roll** of the "shaved-not-buzzed" perf clips is a fresh media-packet go (credits).

---
_Closed by the archivist 2026-07-01 (clearing a prior receipt debt — the director's cut landed at `60b297d` before P-017 but was never receipted). P-016 replaced the 70s rough cut with a late-90s / 2000s bleach-bypass rain-ballad director's cut: 24 new reference-anchored Higgsfield clips (buzz-cut/fit/soaked 2000s frontman + a consistent memory woman + atmosphere; ~198 credits), deterministically sequenced by `scripts/build_edit_v2.py` to the lyrics + beat grid into `data/edl.csv` = **107 cuts, 274.00 s, no clip repeating within 8 cuts**; preflight PASS 0 warnings. The old 70s edit is preserved as `data/{edl,clips,stills}.70s.csv`; `TREATMENT.md` is the director's treatment; frontman likeness corrected across 3 anchor iterations (canonical still `00d037d0`). qa PASS (preflight 0-critical / 0-warning at 107/274.00). reviewer PASS — verify via Higgsfield `video_analysis` (8 character clips on-model); caveats: a few perf clips read "shaved" not "buzzed" (possible re-roll), AI lip-sync not frame-accurate. Base / commit `60b297d`; local-only, awaiting an explicit push go._
