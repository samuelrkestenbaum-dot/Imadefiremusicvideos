# Receipt — P-003: Section-Time Proposal (`when-it-rains/SECTION_TIMES.md`)

- **Date:** 2026-06-29
- **Authority:** marketing-media (document-only — a confirm/correct worksheet; it
  does NOT change the edit).
- **Route:** builder → reviewer → archivist (plus an independent qa numeric pass
  run by the main loop).
- **Branch base (merge-base):** `af8d9fe` on
  `claude/when-it-rains-music-video-fetr0z` (repo has no trunk; "green" is judged
  against the branch tip; `af8d9fe` is the P-002 close commit).

## Scope
- **In:** One new doc — `when-it-rains/SECTION_TIMES.md` (148 lines) — a
  confirm/correct worksheet that proposes section starts for the song so the user
  can lock them. **6 KEEP** (INTRO, V1, V3, CH2, BRIDGE, FINAL) +
  **5 SUGGESTED** (V2 → 0:41, PRE1 → 1:10, CH1 → 1:29 [or 1:51], V4 → 2:24.5,
  PRE2 → 2:53). Feeds the future beat-lock packet.
- **Out (explicit):** ZERO product/pipeline edits. The builder did NOT touch
  `data/edl.csv`, `clips.csv`, `stills.csv`, `assets.json`, any `scripts/`,
  assets, or `analysis/` outputs. No `build-os/` or `.claude/` edits by the
  builder. No Higgsfield generation/upscale (credits = external mutation). No
  push/merge/deploy. **The edit is NOT changed** by this packet — it only proposes
  times for the user to confirm.

## Commits
- `39f84d7` Add SECTION_TIMES.md — data-driven section-start proposal for
  confirm/correct (single commit; green in isolation — a self-contained doc).
- Diff `af8d9fe..39f84d7`: a single changed path
  (`when-it-rains/SECTION_TIMES.md`), 148 insertions.

## QA proof — GREEN (9/9 checks)
- **EDL starts:** all **11** EDL section starts independently re-derived from
  `analyze_song.py` — all match.
- **Transitions / mix end:** all **12** transitions + `music_end 273.75` /
  `duration 284.4` / `tempo 61.5234375` independently match `song.json`.
- **Beat length:** **0.97524s** per beat (61.5234375 BPM → 60/61.5234375),
  correct to 5-decimal-place rounding.
- **KEEP/SUGGESTED flags:** **6 KEEP + 5 SUGGESTED** flags all correct.
- **"Your call" column:** blank (left for the user to fill in), as designed.
- **Honesty caveat present:** the energy≠downbeat caveat is included.
- **Transition reconciliation:** the 12-vs-10 transition mismatch is resolved
  honestly (111.0 + 272.5 identified as mid-section swells, not section starts).
- **Scope:** the change is the single new file `when-it-rains/SECTION_TIMES.md`;
  **zero mismatches** found anywhere in the numeric re-derivation.
- **Working tree:** clean (both before and after the close commit).
- **Safety grep:** clean — no secrets, no live code, benign doc text only.
- (No software test suite exists; this is a marketing/media doc deliverable — the
  doc is itself the decision instrument for the user's section-time confirmation.)

## Review — PASS
- **Verdict:** PASS. No fixes required.
- **Zero fabrication:** every gap/move figure was independently recomputed — no
  number in the doc is asserted without a recomputed basis.
- **Intellectual honesty is its strongest feature:** the energy≠downbeat caveat;
  the 12-vs-10 transition mismatch resolved honestly (111.0 + 272.5 flagged as
  mid-section swells rather than forced into section boundaries).
- **Coupling reduced cleanly:** the CH1/PRE1 coupling is correctly reduced to a
  single user question — **1:29 vs 1:51** for the CH1 chorus downbeat — which also
  settles PRE1.
- **Codex second-eyes: UNAVAILABLE** — single-reviewer pass. Recorded as a caveat.

## Residue
- `when-it-rains/SECTION_TIMES.md` now exists as the **decision instrument** for
  the ambiguous mid-song section starts. The gap stays open until the user
  answers — especially the **CH1 chorus-downbeat call (1:29 vs 1:51)**, which also
  settles PRE1. Until confirmed, the beat-lock packet cannot start.
- **In-session audio-analysis capability now exists:** `numpy` + `imageio-ffmpeg`
  are installed and the song MP3 has been re-attached + staged (gitignored);
  `analyze_song.py` was re-run and the analysis verified **reproducible
  in-session**. (The MP3 itself still will not survive into a new session.)
- Carried-forward deferred packets (see `build-os/memory/residue.md`): beat-lock
  the EDL — snap the 76 cuts to the **0.97524s** beat grid + trim ~2s off FINAL so
  the 276s EDL resolves on the 274s mix end — blocked on the user's confirmed
  section times; band-coverage batch (~8 clips) to reduce C02's 11× overuse (media
  authority, after the lead is locked); selective 2K/4K upscale (last).
- Known risks unchanged: regenerated clips verified by automated re-analysis, not
  human eye; C04 head-turn still absent; lead face varies slightly clip-to-clip;
  no render possible in the cloud env (CDN egress-blocked, no system ffmpeg) — the
  rough cut must be rendered on the user's Mac.

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` — local
  commits only (this closing receipt commit included) without explicit go.
- **No Higgsfield generation / upscale** — credits = external mutation; STOP
  unless inside a confirmed media packet with go.
- **The edit is not yet changed** — SECTION_TIMES.md only proposes times. The next
  packet (beat-lock: edit `data/edl.csv`) awaits the user's confirmed section
  times from SECTION_TIMES.md, especially the 1:29-vs-1:51 CH1 call.
