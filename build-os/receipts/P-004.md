# Receipt — P-004: Section-sync the EDL to confirmed section times (`data/edl.csv` rewrite)

- **Date:** 2026-06-29
- **Authority:** marketing-media (this packet TOUCHED media-pipeline product data —
  `data/edl.csv` — which is allowed *inside* a confirmed marketing/media packet).
- **Route:** builder → reviewer → qa (independent numeric re-derivation) → archivist.
- **Branch base (merge-base):** `bbcc36f` on
  `claude/when-it-rains-music-video-fetr0z` (repo has **no trunk**; "green" is judged
  against the branch tip; `bbcc36f` is the P-003 close commit).

## User input that unblocked this packet
The user **CONFIRMED** the P-003 section-time proposal ("those look good, keep
going"). This **resolves** the prior "awaiting the user's confirm" state from P-003.
Confirmed section starts applied:
- **CH1 chorus downbeat = 1:29 (89.25s)** — the 1:29-vs-1:51 question is settled at 1:29.
- **PRE1 (settling) = 1:09.75** (69.75s).
- **V2 = 41.25s**, **V4 = 144.5s**, **PRE2 = 173s**.
- The **5 anchored sections** (INTRO, V1, V3, CH2, BRIDGE, FINAL) unchanged.

## Scope
- **In (exactly 3 files):**
  - `when-it-rains/scripts/resync_edl.py` — **new**, a deterministic / idempotent
    generator that section-syncs the EDL to the confirmed section times.
  - `when-it-rains/data/edl.csv` — **regenerated**, section-synced, total **274.0s**.
  - `when-it-rains/data/edl_original_backup.csv` — **new**, a **byte-exact** backup
    of the original EDL (the section-sync is fully reversible from this file).
- **Out (explicit):** no other `when-it-rains/**` edits — did NOT touch `clips.csv`,
  `stills.csv`, `assets.json`, other `scripts/`, assets, or `analysis/` outputs.
  No `build-os/` or `.claude/` edits by the builder. No Higgsfield generation /
  upscale (credits = external mutation). No push / merge / deploy.
- **Reversibility guarantee:** the change is fully reversible — restoring
  `data/edl_original_backup.csv` over `data/edl.csv` returns the original cut
  byte-for-byte (backup is byte-identical to the pre-packet original).

## Commits (≤2, base `bbcc36f`)
- `0ac19d2` Add resync_edl.py — deterministic EDL section-sync to confirmed times
  (1 file, +261; **Commit-1 green in isolation** — the script alone reproduces the
  Commit-2 artifacts byte-for-byte).
- `72dff55` Section-sync edl.csv to confirmed section times (274s); back up original
  (2 files: `data/edl.csv` reworked 148±, `data/edl_original_backup.csv` +77).

## QA proof — GREEN (10/10 + Commit-1 isolation, all independently re-derived)
- **Backup integrity:** `data/edl_original_backup.csv` is **byte-identical** to the
  original EDL (sha256 `786a51b1…`).
- **Cut count / order:** **76 cuts**, original row order **preserved**.
- **Zero non-duration field drift:** **ZERO** changes to any non-duration field —
  only durations were re-timed.
- **Per-section sums == target spans:** every section's summed cut durations equal
  its confirmed target span.
- **Cumulative starts == confirmed times:** cumulative section starts land exactly on
  the confirmed times — **including CH1 at 89.25s** (1:29).
- **Total runtime == 274.0s** (resolves on the Jun-27 mix end).
- **Deterministic / idempotent:** re-running the generator yields identical output.
- **Assembler-compatible:** all cut durations fall in the **1.6–8.2s** range
  (`assemble_rough_cut.sh`-safe).
- **Commit-1 reproduces Commit-2:** running `resync_edl.py` (Commit-1) regenerates the
  Commit-2 `edl.csv` + backup **byte-for-byte**.
- **Scale factors applied (per stressed section, recorded for traceability):**
  **CH1 ×1.55**, **V3 ×0.532**, **PRE2 ×1.64**, **FINAL ×0.867**.
- **Safety grep:** clean — no secrets, no live-code injection, benign data only.
- (No software test suite exists; this is a media-pipeline data deliverable. "Proof"
  is the independent numeric re-derivation above + the user's eventual render.)

## Review — PASS (verified by execution + independent re-derivation)
- **Verdict:** PASS. No fixes required.
- **Logic correct:** the section-sync re-times each section's cuts to its confirmed
  span; last-cut residual stays sane even in stressed sections
  (V3 last cut **2.65s**, CH1 last cut **4.65s**, PRE2 last cut **6.56s**).
- **Idempotent + fully reversible:** re-run is stable; the backup's md5 matches the
  base file, so the original cut is recoverable exactly.
- **Honest header:** the script header is accurate — this is **section-sync** (cuts
  re-timed to match confirmed section spans), **NOT** sub-beat / beat-grid
  quantization. No overclaim.
- **Codex second-eyes: UNAVAILABLE** — single-reviewer pass (recorded as a caveat).
- **Non-blocking note (→ residue):** the CH1 (×1.55) / PRE2 (×1.64) **ballooning
  caveat** — those sections were stretched, so their cuts now hold ~5–6.5s and may
  DRAG on playback — currently lives **only in `resync_edl.py` stdout**, not in any
  committed artifact. Surfaced in `build-os/memory/residue.md` for visibility.

## Residue (recorded in `build-os/memory/residue.md`)
- **CH1 (×1.55) + PRE2 (×1.64) cuts now hold ~5–6.5s and may DRAG** — the likely fix
  is **added cuts** (band-coverage batch, `RENDER_REVIEW.md` §7), **not** stretched
  holds. This caveat presently exists only in `resync_edl.py` stdout — surfaced here.
- **V3 (×0.53) cuts now ~1.6–2.7s (fast)** — verify on render that they don't feel rushed.
- **Sub-beat beat-grid quantization still deferred** — needs a rigorous downbeat phase
  reference; section-sync (this packet) is the coarser, confirmed-times pass.
- **Section-sync is reversible** via `when-it-rains/data/edl_original_backup.csv`.

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` (no trunk) —
  local commits only (this closing receipt commit included) without explicit go.
- **No Higgsfield generation / upscale** — credits = external mutation; STOP unless
  inside a confirmed media packet with go.
- **Now awaits the user's render-judgment** of the re-timed cut on their Mac
  (`scripts/fetch_assets.sh` → `scripts/assemble_rough_cut.sh`). The cloud session
  cannot render (CDN egress-blocked, no system ffmpeg).
