# Receipt — P-014: Stills-catalog backfill — make the manifest truthful (close AUDIT-001 / D5-M3)

- **Date:** 2026-06-29
- **Branch base (merge-base):** `00af4e3` (on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip). Chain: `00af4e3` ← `6005114` ← `2223893` (HEAD).
- **Authority:** marketing-media (data / catalog edit on `when-it-rains/data/` + the inline manifest; the user explicitly authorized this — "yes do it"). Route: builder → reviewer → qa → archivist.

## Scope
- **In:** Make the stills catalog **truthful** by closing AUDIT-001 D5-M3 — the catalog must reflect the live Higgsfield assets the regenerations actually produced.
  - **Backfill 11 reference-anchored regen stills** into `data/stills.csv` + `data/assets.json` (10 regen stills surfaced by the audit, plus C04's real source still). UUIDs + CDN URLs retrieved **READ-ONLY** from Higgsfield (no generation, no credits spent).
  - **Resolve C04's `source_still`** from the `regenA1` placeholder to the real still `2d0ba697` in `data/clips.csv` + `data/assets.json`.
  - **Prune 4 superseded off-model stills** (`22e02845` / `b0f3fd47` / `26443c5f` / `ff3797c3`) from the catalog.
  - **Fix the counts:** `data/assets.json` stills **52 → 59**, `counts.stills` **59**; `HANDOFF.md` stills **52 → 59**.
- **Out (explicit):** No Higgsfield **generation** / upscale (read-only retrieval of existing UUIDs only — zero credits). No re-timing / re-cutting of the edit (`edl.csv` untouched). No change to any clip's `mp4_url` or `job_id` (only C04's `source_still` field changed in `clips.csv`). EX1 / EX2's `'prior'` pointers left unresolved **by design** (they point to a prior project — benign). No push / merge / deploy.

## Retrieval method (read-only — no credits)
- The 11 stills' UUIDs + CloudFront URLs were retrieved **READ-ONLY from the Higgsfield source** (querying existing generated media), **not** by regenerating anything. **Zero credits spent** — this is a catalog edit, not a media generation.
- **C04 `source_still` proof (CONFIRMED, not inferred):** C04's clip is Higgsfield job `297449bf`. That job's `medias.start_image` == `2d0ba697`, i.e. the still the clip was actually animated from. Timestamp ordering corroborates: the still's ts `012553` **precedes** the clip's ts `012654`. So `regenA1` (a placeholder) → `2d0ba697` is the **confirmed** real source still, not a guess.

## Commits
- `6005114` Backfill stills catalog: +11 regen stills, −4 off-model, C04 regenA1→2d0ba697 (P-014) — `when-it-rains/data/stills.csv` (**+11 regen rows, −4 off-model rows; net +7 → 59 rows**), `when-it-rains/data/clips.csv` (**C04 `source_still` `regenA1` → `2d0ba697`, a single 1-line change — the other 35 clip rows byte-identical**), `when-it-rains/data/assets.json` (stills **52 → 59** + C04 source_still + `counts.stills` 59; 79 ins / 27 del across the 3 files). Base `00af4e3`.
- `2223893` HANDOFF: stills count 52→59 (P-014 backfill) — `when-it-rains/HANDOFF.md`, **2 ins / 2 del** (one file). Base `6005114`.
- Two commits (≤2 per packet). Commit-1 (`6005114`, the catalog edit) stands green in isolation.

## QA proof — GREEN (9/9)
- **Suite:** no software test suite (the deliverable is a rendered video). QA = catalog / manifest correctness checks against the live `data/` files. **9/9 passed.**
  1. **stills.csv structure:** **59 rows × 3 columns**, clean parse.
  2. **11 present / 4 absent:** all 11 backfilled regen stills present in `stills.csv`; all 4 off-model UUIDs (`22e02845` / `b0f3fd47` / `26443c5f` / `ff3797c3`) absent.
  3. **clips.csv surgical:** the C04 `source_still` change is the **only** line changed — the other **35 clip rows byte-identical**.
  4. **assets.json valid:** parses; `stills` array length **59**; `counts.stills` **59**.
  5. **C04 in assets.json:** C04 `source_still` == `2d0ba697`.
  6. **Parity:** all **36** clips present across `clips.csv` / `assets.json`; `edl` array **86**.
  7. **Resolution outcome:** **34 / 36** clip `source_still`s now resolve to `stills.csv` (all C01–C34); only EX1 / EX2 `'prior'` remain intentionally unresolved.
  8. **HANDOFF:** stills count reads **59**.
  9. **Commit-1 isolation + safety grep.**
- **Commit-1 iso:** GREEN — `6005114` (the catalog edit: stills.csv 59 rows clean; 11 present / 4 absent; clips.csv C04-only; assets.json valid 59 / parity 36 / edl 86) stands green on its own, before the HANDOFF commit.
- **Safety grep:** clean — only `when-it-rains/data/stills.csv`, `when-it-rains/data/clips.csv`, `when-it-rains/data/assets.json`, and `when-it-rains/HANDOFF.md` touched; no secrets, no stray edits.
- **UI smoke:** n/a (no UI artifact in this packet). The load-bearing surfaces — the 11/4 backfill, the C04 resolution, the counts, and the 34/36 resolution rate — were independently re-verified against the live data (qa items 1–8).

## Review — PASS
- **Verdict:** PASS.
- **Findings:** Surgically exact — **net +7** to the stills catalog (11 added, 4 pruned) with **zero in-place mutations** of the kept rows; the C04 `source_still` → `2d0ba697` is **corroborated** by the clip's `start_image` (job `297449bf`) plus timestamp ordering, not guessed; parity preserved (all 36 clips, edl 86); the change **closes D5-M3 truthfully** — the catalog now reflects the live assets. **No overreach** — EX1 / EX2 left unresolved by design (a prior-project pointer), no generation, no edit re-time, no surface outside the four files.
- **Codex second-eyes:** **UNAVAILABLE** — single-reviewer pass.
- **Non-defect caveat (URL not live-hit):** the backfilled CloudFront URLs were **not** live-fetched in-session (egress 403 — the CDN is blocked here, as expected). They came from the **read-only Higgsfield source** with the still UUID embedded in the URL, so the URLs are source-derived, not fabricated — but the final live-fetch confidence check is the user's Mac render path.

## Residue
- **D5-M3 / P-010 stills-catalog gap → RESOLVED (P-014).** Previously characterized as declined / non-functional; it is now **actually fixed** — the 10 reference-anchored regen stills are cataloged, C04's `source_still` is resolved to its real still (`2d0ba697`), and the 4 superseded off-model stills are pruned. The catalog (**59 stills**) is now consistent with the live assets; **34 / 36** clip `source_still`s resolve (all C01–C34).
- **EX1 / EX2 `'prior'` pointers — ACCEPTED, not an open gap.** They point at a **prior project** (intentional / benign); leaving them unresolved is by design, not a defect. These are the only two non-resolvers remaining.
- **The render-review is still the open user step.** The full 86-cut / 274.0s edit has not been watched by a human; both review instruments (`preview.html` silent browser pass + `RENDER_REVIEW.md` audio-render checklist) are current at 86/274.
- Prior LOW open items remain as-is: **D1-01** (engine source-repo wording — GATED, outside this project's authority) and **D3-M1** (receipt "not pushed" wording precision). Deferred-by-design, post-approval: sub-beat beat-grid quantization; selective 2K/4K upscale.

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` (repo has no trunk) — local commits only.
- **No further Higgsfield generation / upscale** — credits = external mutation; needs a fresh go inside a media packet. (P-014's retrieval was read-only — zero credits.)
- **Post-approval polish is premature** — sub-beat quantization + upscale are explicitly later, after the user approves the cut.

---
_Closed by the archivist 2026-06-29. P-014 — stills-catalog backfill: 11 reference-anchored regen stills added (UUIDs/URLs retrieved READ-ONLY from Higgsfield — no credits), C04 `source_still` `regenA1` → `2d0ba697` (CONFIRMED via the clip's `start_image`, job `297449bf`), 4 superseded off-model stills pruned, counts fixed (stills 52 → 59). **34 / 36 clip `source_still`s now resolve** (all C01–C34); only EX1 / EX2 `'prior'` (a prior project) intentionally unresolved. qa GREEN (9/9 + Commit-1 isolation), reviewer PASS (Codex unavailable — single-reviewer; CloudFront URLs not live-hit, egress 403 expected — source-derived). Commits `6005114` + `2223893`, base `00af4e3`. AUDIT-001 D5-M3 RESOLVED. Local commits only — not pushed._
