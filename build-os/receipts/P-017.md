# Receipt — P-017: Higgsfield-native lip-sync pipeline (CODE ONLY)

- **Date:** 2026-07-01
- **Branch base (merge-base):** `60b297d` (P-016, on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip). Chain: `60b297d` ← `8dd6c41` ← `eed8ed8` (HEAD).
- **Authority:** build (CODE ONLY — a coded pipeline under `when-it-rains/`; zero credits, zero Higgsfield calls, zero network in the default path). Route: orchestrator → builder → qa → reviewer → archivist.

## Scope
- **In:** A coded pipeline to lip-sync the front-facing performance shots (`PERF_hook` / `PERF_lookup` / `PERF_window`) to the artist's own vocal via Higgsfield **wan2_7** (audio-driven), in three stages:
  - `scripts/slice_vocals.py` — `--analyze-only` writes the durable, committed `analysis/line_map.json` (43 lyric lines snapped to the committed downbeat grid, **27 in-scope** — sung by a front-facing performance shot — embedding the **3 in-scope still job_ids** for `PERF_hook` / `PERF_lookup` / `PERF_window` so the pipeline survives a fresh session after the gitignored `song.mp3` is gone). A full run cuts each in-scope line to a mono 24 kHz WAV via the bundled ffmpeg and writes a deterministic `segments_manifest.json`. Idempotent (byte-identical on re-run).
  - `scripts/lipsync_driver.py` — consumes `segments_manifest.json` and builds the exact **gated** wan2_7 call-plan. Two upload paths: **path-b** (mint `media_upload` here → emit `upload_segments.sh` for a Mac curl-PUT, with a presigned-url expiry / re-mint note → `media_confirm` → wan2_7) and **path-c** (pre-supplied `media_ids` from a web-app upload → wan2_7). `--dry-run` is the **default** and makes ZERO network calls (prints the plan, writes `upload_segments.sh` + `lipsync_plan.json`); `--go` routes to documented, UN-EXECUTED stubs that **raise `SystemExit`** — a build step never generates; the assistant runs live MCP later with an explicit human `--go`.
  - `scripts/swap_lipsync_clips.py` — on demand, **NON-DESTRUCTIVELY** wires produced synced clips in: backs up `data/edl.csv` → `data/edl_pre_lipsync_backup.csv`, adds `<PERF_key>_synced` variants to `clips.csv`, and repoints only the in-scope `PERF_hook` / `PERF_lookup` / `PERF_window` cuts. Deterministic + idempotent; preflight stays PASS.
- **Out (explicit):** NO live Higgsfield generation / credits (the `--go` path is stubbed and refuses by `SystemExit`). NO network in the default path. `song.mp3` NOT committed (gitignored); `audio_segments/` (the WAVs + `segments_manifest.json`) gitignored — `analysis/line_map.json` is the durable committed artifact. No mutation of the live edit at build time (the swap is on-demand and reversible). No push / merge / deploy.

## Commits
- `8dd6c41` **P-017 Commit 1: Higgsfield lip-sync stage 1 — line-map + vocal slicer** — `analysis/line_map.json` (532), `scripts/slice_vocals.py` (373), `scripts/test_slice_vocals.py` (305), `.gitignore` (+5). **4 files, 1215 insertions.** Base `60b297d`. Green in isolation (driver/swap checks SKIP — added in Commit 2).
- `eed8ed8` **P-017 Commit 2: lip-sync driver (dry-run/gated) + non-destructive clip swap** — `scripts/lipsync_driver.py` (272), `scripts/swap_lipsync_clips.py` (148), `scripts/test_slice_vocals.py` (+18, driver/swap checks now active). **3 files, 438 insertions.** Base `8dd6c41` (tip).
- Two commits (≤2 per packet). Across both: `git diff --stat 60b297d..eed8ed8` = **6 files, +1653 insertions**. Commit-1 stands green in isolation.

## QA proof — GREEN
- **Full suite (at tip):** `test_slice_vocals.py` **42 passed / 42 total / 0 failed** — with the **full-slice branch executed** (song.mp3 present). The suite restores the live CSVs so it leaves the tree byte-identical.
- **Commit-1 green in isolation:** at `8dd6c41` the suite is **23 passed / 0 failed / 4 skipped** (driver/swap checks SKIP — those files land in Commit 2). Commit-1 builds and passes on its own.
- **Gated-generation safety:** **zero-network dry-run** verified on BOTH paths (path-b and path-c) — the default `--dry-run` makes no network calls (checked under a socket guard, no top-level net import); `--go` is **refused** (routes to stubs that raise `SystemExit`). Nothing spends without a human `--go` + the live MCP.
- **Non-destructive swap:** `swap_lipsync_clips.py` backs up `edl.csv` → `edl_pre_lipsync_backup.csv` and repoints only the in-scope cuts; `preflight_edl.py` **PASS before AND after** the swap; swap is deterministic + idempotent.
- **Safety grep — clean:** no secrets; `song.mp3` gitignored (untracked); `audio_segments/` gitignored (WAVs + manifest regenerated locally, never committed); `data/` empty of generated artifacts. `git diff --stat 60b297d..eed8ed8` = **6 files, +1653**.
- **UI smoke:** n/a (no UI artifact). Load-bearing surfaces — the dry-run zero-network invariant, the `--go` refusal, and the reversible swap with preflight PASS — re-verified above.

## Review — PASS
- **Verdict:** PASS (reviewer, this session).
- Gated-generation safety airtight (dry-run zero-network on both paths; `--go` `SystemExit`); no hardcoded md5; the swap is reversible (backup file) and preflight-clean before and after; the code is honest about its assumed bits (wan2_7's `audio_references` shape is flagged to reconcile at the single gated validation); trajectory low-risk (nothing spends at build time). Commit-1 isolation confirmed.
- **Codex second-eyes:** UNAVAILABLE this session — **reviewer ran SOLO** (single-reviewer).

## HONEST VALIDATION STATUS (critical)
- The P-017 **CODE is correct and shelf-ready.** What is UNPROVEN is the underlying **wan2_7 audio-driven lip-sync MECHANISM**, which is being validated live this session and is **SO FAR FAILING**:
  - **3 wan2_7 render attempts FAILED outright.** Inputs resolve (`start_image` + audio attach fine), but the generation itself dies. Tried durations 8 / 5 / 5 s and both a long and a short TTS audio.
  - A **4th attempt** using `media_import_url`'d proper `media_id`s (image `686b2b8a`, audio `720e0e5a`) is **in flight at close time.**
- **If the 4th also fails, the finding is:** wan2_7 does **not** reliably produce audio-driven lip-sync output for our stylized dark performance stills, and the recommended path for true lip-sync becomes **shooting real performance footage of the artist** (graded + intercut). Recorded prominently in `residue.md`.

## Residue / caveats (recorded to residue.md)
- **wan2_7 lip-sync validation is FAILING (3–4 attempts).** If unproven, true lip-sync likely needs **filmed footage**, not AI generation. The P-017 code stays shelved-ready regardless.
- **`song.mp3` still gitignored / session-only** — the full-slice branch (and any live slicing) needs `song.mp3` re-attached in `when-it-rains/`; `analysis/line_map.json` is the durable artifact.
- **wan2_7 `audio_references` shape is assumed** in `lipsync_driver.py` and flagged to reconcile at the single gated validation.
- **The clip swap is deferred-by-design** — `swap_lipsync_clips.py` is on-demand and only runs after real synced clips exist; the live edit is untouched at build time.

## Open boundaries (awaiting explicit go)
- **NOT YET PUSHED:** commits `8dd6c41`, `eed8ed8` (P-017) and the archivist's own `build-os/` close commit are **local-only**, awaiting the user's explicit push go. (Also unpushed: P-016 chain — see residue.)
- **No live wan2_7 generation / credits** without a human `--go` + the live MCP.
- **No push / merge / deploy** on `claude/when-it-rains-music-video-fetr0z` without explicit go.

---
_Closed by the archivist 2026-07-01. P-017 shipped a Higgsfield-native (wan2_7 audio-driven) lip-sync pipeline as CODE ONLY: `slice_vocals.py` (durable `analysis/line_map.json` — 43 lines, 27 in-scope, 3 still job_ids embedded) + `lipsync_driver.py` (gated wan2_7 call-plan; DRY-RUN default, `--go` refuses by `SystemExit` — spends nothing without a human go + the MCP) + `swap_lipsync_clips.py` (non-destructive, reversible, preflight-clean). qa GREEN (suite 42/42/0 with the full-slice branch executed; zero-network dry-run both paths, `--go` refused; non-destructive swap, preflight PASS before+after; Commit-1 green in isolation 23/0/4; safety clean, no secrets, song.mp3 + audio_segments/ gitignored; diff 6 files / +1653, data/ empty). reviewer PASS (gated-generation safety airtight, no hardcoded md5, reversible swap, honest about assumed bits) — Codex unavailable, solo. Base `60b297d`; tip `eed8ed8`. **HONEST STATUS:** the CODE is correct and shelved-ready, but the wan2_7 mechanism is UNPROVEN and SO FAR FAILING (3 render attempts failed; a 4th with proper media_ids in flight at close) — if it also fails, true lip-sync likely needs filmed performance footage. P-017 commits + this close are local-only, awaiting an explicit push go._
