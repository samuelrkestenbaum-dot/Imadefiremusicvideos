# Receipt — P-022: Likeness repair + anchor board + ladder inserts

**Status:** CLOSED — delivered first70.mp4 + v1_v2 + v2_v2 (2026-07-03)
**Written by:** main loop (archivist fallback still in effect).

## What shipped
- `review/ANCHOR_BOARD.png` — permanent likeness reference (hero clip frame +
  bed master + kitchen master face crops). Committed; all future faces compare
  against it in QC.
- Candidate-batch repair (count=4 per still, user-confirmed picks DOOR 3 +
  CURB 3): street door master `14976025` -> scrub `97bf512d`; curb master
  `4c67c1b4` -> scrub `afe502ae`. Chain removed on BOTH (Soul bias again);
  distant pedestrians removed from the door still.
- Repaired takes: door `c742b889` (look measured 4.0-6.5), curb `896f8dfb`.
- LADDER INSERTS (roadmap Part 3): V1 window she-smear — ghost still
  `e16c04b4` -> take `28668737`, blended at 65% over the plain take (45% was
  invisible; caught in final QC, re-rendered free); V2 puddle her-reflection —
  still `4358f99e` -> take `31ccf929` (reflection ripples apart on cue ~3s;
  cut at in 2.2 for a ~0.7s sighting).
- Recuts `render_v1_v2.sh` / `render_v2_v2.sh` + `render_p022.sh` wrapper
  (one CI run renders both sections + the first70 stitch — new pattern).
- `first70.mp4`: continuous 0:00-70.3 (intro_v6 + v1_v2 + v2_v2, one audio).

## Notes
- V2's cut simplified: the door take now plays 7.8s unbroken (out -> walk ->
  look -> walk on) — patient 90s grammar, look lands ~song 45.8-48.3.
- Curb hold wardrobe: hoodie under jacket (matches the awning sync; the
  section reads as a layering progression, watch for user reaction).
- Spends: 8 candidate stills, 4 scrub/ghost nano edits, 4 kling takes.
  Estimate remaining: ~1150-1300 of 2415.

## Next
P-023 PRE1 (70.3-89.8): the darkening walk. Board first per the roadmap
story rules (Δ + because-chain stated before any generation).
