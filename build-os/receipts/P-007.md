# Receipt — P-007: Insert band-coverage C25–C34 into the EDL + re-balance PRE2/CH1

- **Packet id:** P-007
- **Title:** Insert band-coverage C25–C34 into the EDL + re-balance PRE2/CH1
- **Date:** 2026-06-29
- **Authority:** marketing-media (media-pipeline edit on `when-it-rains/**`
  product files). Route: builder → reviewer → qa → archivist. The gated edit
  followed the user's explicit go.
- **Branch base:** `claude/when-it-rains-music-video-fetr0z` (no trunk; judged
  against branch tip). Packet base **`ed046b5`** (P-006 close commit). HEAD at
  close **`1969435`**.

## Scope

- **In:** Splice the 10 P-006 band-coverage clips into the edit kit — C25–C30 into
  PRE2, C31–C34 into CH1 — via a deterministic/idempotent inserter; regenerate
  `data/edl.csv`; write the new clip rows into `clips.csv` (+10) and `stills.csv`
  (+10); re-time PRE2 / CH1 to ~3.3s avg. Preserve a fresh pre-edit backup.
- **Out (explicitly):** Any further Higgsfield generation / upscale (still gated;
  upscale stays last). The C27/C33 on-model confirmation (deferred to the user's
  render-review). The human-eye render review (off-machine, user's Mac). No
  push / merge / deploy.

## Commits (this packet)

- **`1969435`** — `Insert band-coverage C25–C34 + re-balance PRE2/CH1
  (insert_band_coverage.py)`. Base `ed046b5`. Single commit (≤2 satisfied).
  Files (all under `when-it-rains/`):
  - `scripts/insert_band_coverage.py` (new, +434) — deterministic / idempotent
    inserter.
  - `data/edl.csv` (regenerated → **86 cuts**).
  - `data/edl_pre_bandcoverage_backup.csv` (new, +77) — byte-identical pre-P-007
    EDL backup.
  - `data/clips.csv` (+10 rows, C25–C34).
  - `data/stills.csv` (+10 rows, C25–C34).
  - Stat: 5 files changed, 633 insertions(+), 92 deletions(-).
- Closing commit (this receipt + the 3 memory files) recorded by the archivist at
  commit time — touches only `build-os/`.

## What changed (PRE2 / CH1 before → after)

- **PRE2: 6 → 12 cuts**, spanning **41.00s** (unchanged total span). New shape =
  an **8.2s lead** + five **6.56s holds** broken down to **~3.4s avg per cut**
  (lead trimmed to **4.12s**). C25–C30 (declined_preset 24bae836-… = ON,
  storm/dark) woven in after rows 51–56.
- **CH1: 8 → 12 cuts**, spanning **38.75s** (unchanged total span). New shape =
  a **6.2s lead** + 4.65s holds broken down to **~3.1–3.2s per cut**. C31–C34
  (declined_preset OFF / warm) woven in after rows 29, 31, 33, 35.
- **The PRE2/CH1 drag from P-004 (sections stretched ×1.64 / ×1.55, holding
  ~5–6.5s) is RESOLVED** — the C12 / C10 / C02 recycling is broken and **no two
  new cuts land back-to-back**.
- **Totals preserved:** total still **274.0s**; **86 cuts** indexed 1..86; all
  **12 section starts unchanged**; the other 9 sections untouched.

## QA proof — GREEN (13/13 independently re-derived)

1. **Scope = 5 files** (the exact files above) — no out-of-scope edits.
2. **Backup fidelity:** `data/edl_pre_bandcoverage_backup.csv` is **byte-identical**
   to the pre-P-007 `data/edl.csv`.
3. **Cut count / indexing:** **86 cuts**, index **1..86** contiguous.
4. **PRE2 / CH1 spans exact:** PRE2 = **41.00s**, CH1 = **38.75s**.
5. **Other 9 sections unchanged** (span-for-span).
6. **Cumulative section starts** — all **12 unchanged** from pre-P-007.
7. **Total = 274.0s** (unchanged).
8. **62 non-PRE2/CH1 rows byte-identical** to the backup.
9. **Insertion sequences exact** — clips land in the planned slots; **no two new
   adjacent**.
10. **`clips.csv` +10** matching the asset map (job_id, source_still =
    still_id[:8], section = band, mp4_url, still_url, prompt_summary).
11. **`stills.csv` +10** matching the asset map (same key fields).
12. **Durations 1.6–5.0s** — assembler-safe; watchable mins **3.11s / 3.30s**.
13. **Deterministic / idempotent** re-run reproduces the same EDL; **safety grep
    clean** (no secrets / stray markers).

- **Commit-1 isolation:** single-commit packet; the one commit builds the 86-cut
  EDL and asset rows on its own (no software test suite — proof is the re-derived
  counts above).
- **UI smoke:** N/A — this is a data/media-pipeline packet, no UI surface.

## Reviewer verdict — PASS

- Script logic **sound + idempotent**; editorial result **lands the fix** (build /
  chorus pacing restored, watchable min durations **3.11s / 3.30s**).
  Registry / asset fidelity spot-checked. Fully **reversible**
  (`data/edl_original_backup.csv` was NOT overwritten). **No fixes required.**
- **Codex second-eyes: UNAVAILABLE** — single-reviewer pass (caveat recorded).

## Reversibility

- **`data/edl_pre_bandcoverage_backup.csv`** — byte-identical pre-band-coverage
  EDL (the 76-cut, section-synced 274.0s state). Restoring it over `data/edl.csv`
  returns the pre-P-007 cut byte-for-byte.
- **`data/edl_original_backup.csv`** — INTACT / not overwritten — the byte-exact
  pre-section-sync (pre-P-004) original 276s EDL still stands.
- **Reversibility chain:** P-007 → `edl_pre_bandcoverage_backup.csv` (pre-band) →
  `edl_original_backup.csv` (pre-section-sync). `scripts/insert_band_coverage.py`
  and `scripts/resync_edl.py` are both deterministic / idempotent, so each layer
  can be regenerated exactly.

## Scope guarantee

- The packet commit `1969435` touched **only** `when-it-rains/` product files (the
  5 listed). The closing commit touches **only** `build-os/`. No push / merge /
  deploy.

## Residue / follow-ups

- **C27 + C33 on-model NOT auto-verified** — confirm at the user's render-review;
  regenerate just those (~10 credits) if off-model. Risk low (reference-anchored).
- **Sub-beat beat-grid quantization** (0.97524s grid, 61.5234375 BPM) — still
  optional / deferred; needs a rigorous downbeat phase reference + the MP3
  re-attached.
- **Human-eye render review still pending** — the rough cut (86 cuts, 274.0s)
  must be rendered on the user's Mac (`scripts/fetch_assets.sh` →
  `scripts/assemble_rough_cut.sh`), walking `when-it-rains/RENDER_REVIEW.md`.

## Open boundaries (pending explicit go)

- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` — local
  commits only.
- **No further Higgsfield generation / upscale** — credits = external mutation;
  gated (including the ~10-credit C27/C33 regen if off-model).

---
_Closed by the archivist 2026-06-29. The edit is COMPLETE for this round: 86 cuts,
PRE2/CH1 pacing fixed to ~3.3s avg, band coverage C25–C34 woven in, total 274.0s
— ready for the user's render-review. qa GREEN (13/13), reviewer PASS
(Codex second-eyes unavailable)._
