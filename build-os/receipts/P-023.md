# Receipt — P-023: first70 revision (mug grain, lip-sync timing, AI-look syncs, curb finale redesign)

**Status:** CLOSED — first70.mp4 v2 delivered, final frame-QC verdict **SHIP** (2026-07-03)
**Written by:** archivist (subagent back in service; P-020/P-022 receipts were
main-loop fallback — see Notes).

## Scope

- **In:** revise the P-022 first70 per the user's four notes on v1 — (1) grainy
  mugs, (2) late lip-sync onset, (3) AI-look sung shots, (4) redesign the curb
  finale. Media packet under `when-it-rains/` (scripts, review QC, CI renders,
  Higgsfield spends inside the packet go).
- **Out (explicit):** the LOCKED intro_v6 and chorus_v16 (untouched — including
  the known jewelry flag inside the locked intro, recorded below); PRE1 and all
  later sections; any merge/deploy/publish/secrets; the training-photo history
  purge (still user-gated, unanswered).

## What shipped

- **`first70.mp4` v2** — continuous 0:00–70.3 (70.0s rendered), final 140-frame
  frame-QC pass = **SHIP**. Section recuts: **`v1_v2.mp4`** (19.5s),
  **`v2_v2.mp4`** (27.3s). Curb zoom finale occupies 58.6–70.3: a 12s sync, the
  world rushing behind him, accelerating push-in + vignette + desat ramp
  (zoompan chain in `render_v2_v2.sh`).
- Branch: **`claude/when-it-rains-handoff-l0u075`** (supersedes `...-fetr0z`,
  same history). `render-chorus.yml` is now **branch-agnostic** — checks
  out/pushes `${{ github.ref_name }}`, the relay follows whatever branch
  triggered it.

## The four user notes on first70 v1 — all resolved

1. **Grainy mugs** → 2K-enhanced mugs take `82720c7f` swapped into
   `render_v1_v2.sh` (file `hf_20260703_014921_82720c7f-...mp4`).
2. **Late lip-sync onset** → onset-tight vocal slices + new wan syncs, onset on
   the first beat: kitchen wan `552f47fe` → 2K `c3cddeb8` (from imported still
   `08515f10` = scrubbed pick `d52d9aeb`; audio `d68483dc` v1_line3_tight, song
   31.2+4.3s); awning **retry** wan `ee8d1c61` → 2K `ab86a6c8` (first awning wan
   `3397e6b4` REJECTED in frame-QC — singing stopped at 2.5s; retry prompt
   demands CONTINUOUS singing; from imported still `4953e937` = scrubbed pick
   `a919b037`; audio `5158896a` v2_line7_tight, song 54.6+4.3s).
3. **AI-look sung shots** → photorealism stills `d52d9aeb` (kitchen) /
   `a919b037` (awning), QC-passed, likeness 8.5/7.5.
4. **Curb finale redesign** → 12s curb zoom sync: wan `da598028` → 2K
   `2ad7d2e3` (from imported media `eac51105` = `review/curb_final.png`; audio
   `979f0f26` v2_line8, song 58.5+11.9s); accelerating zoompan + vignette +
   desat ramp in `render_v2_v2.sh`.

## The curb frame saga (records the new hard rule)

- The 3rd soul_2 roll failed wardrobe AGAIN — bare chest ×3, 90° rotations ×2.
- **USER ORDER:** all bare-chest images deleted immediately. Repo copies purged
  from **git HISTORY** (commit rewrite + force-push — user-ordered, hence the
  explicit-go bar was met). Higgsfield-library copies `733152dc` / `8c9b80ef` /
  `85cb2d58` **cannot be deleted via MCP** — the user must delete them in the
  Higgsfield UI (still outstanding, see Residue).
- **Salvage, don't reroll:** C5 (`85cb2d58`) salvaged — nano `bcf30501` (zip
  jacket, remove chain/ring/sign) → nano `12d07ba3` (zip to collar, hand
  glints) → **user-approved pass-3 exception** (face held 9/10 across all
  passes) → final 2 residual hand glints removed **LOCALLY** by deterministic
  one-sided luminance suppression (168 px touched, face byte-identical) — QC
  CLEAN → `curb_final.png` (imported as media `eac51105`).

## New law candidate — audio-gap verification

wan honors vocal breath gaps: before failing a take for "mouth stops moving",
verify the QC flag against the slice's mid-band FFT energy envelope (per-100ms,
imageio-ffmpeg decode). Both flagged stretches in the 12s curb take matched
real energy dips exactly (1.5–3.0s, 6.5–7.0s) — the take was CORRECT sync, not
a defect. Canonized in HANDOFF ops notes; candidate for the playbook.

## Proof (frame-QC via vision subagent — sandbox eyes before every spend)

- Every asset frame-QC'd BEFORE each spend: the 5-still batch; kitchen/awning
  raw wans (caught the `3397e6b4` static-after-2.5s failure pre-2K); curb
  pass-2/pass-3 stills; painted patch ×2 rounds; the 12s wan (24 frames).
- **Final pass:** 140 frames over first70 v2 = **SHIP**, with 3 non-blocking
  caveats: (a) chain + ring visible ~19–21s inside LOCKED intro_v6 — predates
  P-023; (b) mugs-take face visible — canonical per the anchor board; (c) curb
  zipper mid-chest over hoodie — no skin, wardrobe intent satisfied.
- No software test suite (media packet) — exact counts are the QC passes above.
  Safety: no secrets committed; `song.mp3` stays gitignored; the bare-chest
  purge left the history clean of the offending images.

## Reviewer verdict

- **SHIP** — the final 140-frame vision-QC pass is the shipping judgment
  (accepted process deviation for media packets, precedent P-018/P-020/P-022;
  no formal qa/reviewer agents; Codex second-eyes not run — not applicable to
  the CI-relay media workflow).

## Commits

- Packet began pre-handoff on the fetr0z lineage (`188ed03` onset-tight slices
  → `e06cd89` realism candidates; mid-packet state committed in `abbcdaf`
  HANDOFF).
- This session on `claude/when-it-rains-handoff-l0u075`: **`c1e1efa` →
  `9992e95`** — 18 commits (11 authored relay + 7 CI render commits), plus this
  receipt/close commit on top. Key points: `c1e1efa` (CI relay
  branch-agnostic), `121d81d`/`1211a32` (recut scripts), `c0ad6e0` (curb final
  frame), `d2be7d3` (full recut + restitch), `9992e95` (QC purge + HANDOFF
  refresh).
- **Base:** no trunk / no `origin/main` — green is judged against the branch
  tip; the branch continues the fetr0z history and auto-mirrors on CI pushes.
- **≤2-commit contract: acknowledged inapplicable** to the CI-relay media
  workflow (many small relay commits + CI render commits are the mechanism),
  same precedent as P-020/P-022. Recorded, not silent.

## Credits

1451.59 at session resume → **1405.71 at close** (~46 spent: 2× wan 5s, 1× wan
12s, 3× bytedance 2K, 3× nano, imports). Roadmap estimate for everything
remaining was 950–1100 — comfortable.

## Residue (mirrors HANDOFF.md)

- **Training photos remain in PUBLIC git history** — purge offered, still
  unanswered; re-raised at this close. Needs explicit go (history rewrite).
- **Locked-intro jewelry flag:** intro window sync ~19–21s shows a thin chain +
  ring — predates P-023, inside user-locked intro_v6. Flag to the user before
  FINAL assembly; fixing means reopening the lock. Record only.
- **3 bare-chest curb generations still in the Higgsfield library**
  (`733152dc` / `8c9b80ef` / `85cb2d58`) — MCP cannot delete them; the user was
  told to delete them in the Higgsfield UI.
- **NEW HARD RULE (user, 2026-07-03): NO bare chest, ever.** Any generation
  showing bare chest is deleted immediately (repo copies purged from HISTORY,
  not just tip). Do NOT reroll wardrobe fixes — salvage the best frame via nano
  edit instead (soul_2 ignored "closed and zipped" locks 3 rolls running).
- Max-2-nano-passes law: the 3rd pass was a one-time user-granted exception —
  do not make it habit.

## Open boundaries

- No merge / deploy / publish / secrets without explicit go (CI auto-push of
  render commits on this branch is the established relay mechanism, not a new
  mutation).
- Training-photo history purge: user-gated, unanswered.
- Locked material stays locked: hero `491e39d1`, chorus_v16, intro_v6.
- New Higgsfield generation only inside the next staged packet with go.

## Next

**P-024 — PRE1 (70.3–89.8): the darkening walk.** Board first per the roadmap
story rules (Δ + because-chain stated before any generation). Note: the roadmap
table's packet ids drifted — it lists PRE1 as "P-023", but P-023 was consumed
by this first70 revision; PRE1 is **P-024** and later sections shift by one
(HANDOFF.md concurs).

## Notes (memory hygiene)

`build-os/memory/` had not been advanced since P-018/P-019 — P-020 and P-022
closed receipt-only via the main-loop fallback (archivist usage credits were
exhausted), and **P-021 (V2 board, the street, 43.0–70.3 → `v2_v1.mp4`; commits
`def07ba`..`ecc9bc9`) closed UNRECEIPTED** — its work is superseded/recorded by
the P-022 recut (`v2_v2`) and now this note (receipt debt acknowledged). This
close brings `current_state.md` / `residue.md` / `active_packet.md` back in
sync through P-023.
