# Receipt — P-009: Reconcile assets.json clip entries to clips.csv (manifest sync, zero functional change)

- **Date:** 2026-06-29
- **Branch base (merge-base):** `cb7610b` (on `claude/when-it-rains-music-video-fetr0z`; repo has no trunk — green is judged against the branch tip)
- **Authority:** marketing-media (manifest / docs-consistency sub-scope on `when-it-rains/data/assets.json`; **no generation, no CSV functional change**). Route: builder → reviewer → qa → archivist.

## Scope
- **In:** Bring `when-it-rains/data/assets.json`'s **pre-existing** clip entries for the **11 earlier-regenerated clips** (**C01, C04, C06, C07, C13, C15, C16, C19, C22, C23, C24**) into line with the fixed `data/clips.csv` — updating each clip's `job_id`, `source_still`, and `mp4_url` to the verbatim values the render actually reads from `clips.csv`. The other 25 clip entries were left byte-identical.
- **Out (explicit):** No Higgsfield generation / upscale (credits = external mutation). No change to any source CSV (`clips.csv`, `stills.csv`, `edl.csv` are the source of truth — untouched). No change to the assets.json counts block, runtime, stills array, models, references, or the inline `edl` array (all already reconciled by P-008). No push / merge / deploy. The pre-existing source_still↔stills.csv id-format quirk is **out of scope** (re-characterized as optional P-010 — see Residue).

## Commits
- `f59829b` Reconcile assets.json: 11 regenerated clips now match clips.csv — one file (`when-it-rains/data/assets.json`), **33 insertions / 33 deletions**, single commit, base `cb7610b`.

## QA proof — GREEN
- **Suite:** no software test suite (deliverable is a rendered video). QA = manifest-vs-source reconciliation checks → **8/8 passed**.
  1. assets.json clips array == clips.csv: **36/36 reconcile, 0 divergence** (job_id / source_still / mp4_url all match).
  2. Exactly the **11** named clips (C01, C04, C06, C07, C13, C15, C16, C19, C22, C23, C24) changed; the other **25** clip entries are **byte-identical** to pre-P-009.
  3. Old (pre-regen) job_ids and source_stills for the 11 are **all gone** from assets.json (no stale residue).
  4. stills array (**52**) byte-unchanged; stills array == `stills.csv`.
  5. counts block byte-unchanged (**36 / 52 / 86**); runtime byte-unchanged.
  6. inline `edl` array (**86** rows) byte-unchanged (== `edl.csv` from P-008).
  7. models block + references block byte-unchanged.
  8. Safety grep: clean (no secrets / no stray edits).
- **Commit-1 iso:** single-commit packet — `f59829b` builds/reconciles green on its own (GREEN).
- **Safety grep:** none found.
- **UI smoke:** N/A (no UI; deliverable is a rendered video).

## Review — PASS
- **Verdict:** PASS.
- **Findings:** Verbatim-from-source correctness — the 11 updated entries copy `clips.csv`'s values exactly. Scope airtight: only `when-it-rains/data/assets.json` touched, only the 11 clip entries within it. The manifest now points at the **CORRECT fixed clips** (more truthful than before — it previously carried pre-regen job_ids/urls that diverged from the rendered footage).
- **Key clarification (reviewer):** the `source_still`↔`stills.csv` non-resolution is a **PRE-EXISTING id-format quirk**, NOT a P-009 defect — `clips.csv` stores `source_still` as **8-char prefixes** while `stills.csv` keys on **full UUIDs**, so they don't string-match. This affects **all 36 clips both BEFORE and AFTER P-009** and is non-functional (the render reads `mp4_url` from `clips.csv` directly, not via the still join). Re-characterized as optional/low-priority **P-010** (see Residue).
- **Codex second-eyes:** UNAVAILABLE — single-reviewer pass.
- **Product Trajectory Check:** correct direction — closes the last manifest-drift thread; after P-009 the repo is coherent and the only open work is the user's render-review.

## Residue
- **Deferred / follow-up packets:**
  - **P-010 (optional / low-priority, needs user go):** backfill the `source_still`↔`stills.csv` resolution. `clips.csv`'s `source_still` values are **8-char prefixes** that don't resolve to `stills.csv`'s **full-UUID** keys (a pre-existing id-format quirk affecting all clips; **non-functional** — the render reads `mp4_url` from `clips.csv` directly). Also: **C04's** `source_still` is a `regenA1` **placeholder** (known-incomplete from the earlier C04 partial regen — hair/reflection fixed, head-turn still absent). Touches a source CSV + a count, so it needs an explicit go.
  - Sub-beat beat-grid quantization (optional, deferred) — snap the 86 cuts to the 0.97524s grid; needs a downbeat phase reference + the MP3 re-attached.
  - Selective 2K/4K upscale — explicitly LAST, after the cut is locked.
- **Known risks:** the rough cut still needs a **human render pass** — the 86-cut / 274.0s edit has not been watched by a human (the 11 fixes were confirmed by automated re-analysis; C27/C33 verified on-model via `video_analysis`, NOT a human eye). The user must render on their Mac (`scripts/fetch_assets.sh` → `scripts/assemble_rough_cut.sh`) and walk `RENDER_REVIEW.md`. No render is possible in the cloud env (CDN egress-blocked, no system ffmpeg).

## Open boundaries (awaiting explicit go)
- **No push / merge / PR** on `claude/when-it-rains-music-video-fetr0z` (repo has no trunk) — local commits only without explicit go.
- **No further Higgsfield generation / upscale** — credits = external mutation; needs a fresh go (and would be inside a media packet). P-010, if taken, also needs a go (touches source CSV + a count).
