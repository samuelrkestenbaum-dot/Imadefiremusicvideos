# Receipt — P-024: PRE1 (70.3–89.8) — the darkening walk

**Status:** CLOSED — `pre1_v1.mp4` + `first90.mp4` delivered, final frame-QC
verdict **SHIP** (2026-07-03)
**Written by:** archivist.

## Scope

- **In:** PRE1, the darkening walk (70.3–89.8s) — lines "Don't know if you
  still feel it / I lose my breath / the trees are moving / like you never
  left." Board → user-gated masters → derived takes → sync stack → measured
  cut → frame-exact stitch into a continuous first90. Media packet under
  `when-it-rains/` (scripts, review QC, CI renders, Higgsfield spends inside
  the packet go, given 2026-07-03).
- **Out (explicit):** LOCKED intro_v6 / chorus_v16 / hero `491e39d1`
  (untouched); everything past 89.8; any merge / deploy / publish / secrets;
  the training-photo history purge (still user-gated, unanswered).

## What shipped

- **`pre1_v1.mp4`** (19.51s) — the PRE1 section cut.
- **`first90.mp4`** (89.79s) — continuous 0:00–89.8 on a **frame-exact grid**
  with the **July Reverb mix** as the audio bed. Final frame-QC pass = **SHIP**;
  PRE1 likeness **9/10**.
- Branch: **`claude/when-it-rains-handoff-l0u075`** (no trunk; green judged
  against the branch tip; CI relay branch-agnostic).

## Masters — user-gated picks (board user-approved first)

- **Walk master:** W2 `4aaefb8b` + chain scrub → `361af0a5`.
- **Turn master:** T3 `d1688201` → **LOCAL rotate90CW + chest-up crop**
  (`turn_master_crop.png`, imported `b59d18d8`, **ZERO nano passes** on this
  face). **User chose T3 over the recommended T4** — recorded as the user's
  gate call.
- **Trees:** derived from the scrubbed walk (`b6f2262e`), per law 0 DERIVE.
- **Takes:** walk-stop-breath kling `584f5e2b` (10s); gust kling `8543a623`
  (5s). **Sync:** wan RETRY `97a9771e` (8s) → 2K `c6888a50`.

## The wan re-roll (records the new behavioral-pinning law)

- First sync take **REJECTED in frame-QC**: wan invented staging — he turned
  his back to the camera for **4.5s mid-line** (breaking lip-sync) and a
  warped hand smeared past the lens at the exit.
- **Fix = behavioral pinning**, not luck: prompt pins "sings DIRECTLY INTO THE
  CAMERA... NEVER turns away, NEVER shows the back of his head", confines exit
  choreography to a stated final window, and adds "no hand near the lens". The
  pinned retry (`97a9771e`) worked **first time — 9/10**.

## The stitch drift bug (found and fixed — now law)

- Section video streams run a few frames short of their audio; naive concat
  accumulated **~0.3s cumulative EARLY drift** by 70s. Fixed by **tpad+trim to
  exact frame counts** per segment (`render_p024.sh` pattern: 576/456/655/468).
  **FRAME-EXACT STITCH LAW: apply to every future stitch wrapper.**

## New laws / learnings (canonized in HANDOFF.md)

1. **AUDIO MIX SWAP:** the song is now the **July Reverb mix** (300s file,
   same clock — **verified by envelope cross-correlation** at anchors
   31.2 / 58.5 / 89.8 / 109.3, all **0ms offset**; music rings to ~277s vs the
   old 273.8). ALWAYS cross-correlate anchors before slicing any new mix.
   `song.mp3` stays gitignored.
2. **Frame-exact stitch law** (above).
3. **Wan behavioral pinning** (above).
4. **soul_2 rotation quirk:** chest-up + facing-camera prompts rendered
   ROTATED 90° **4/4** — salvage = **local rotate+crop** (deterministic, no
   face passes) beats rerolling. **kling `sound:"off"` must be passed
   explicitly** (2 takes rendered sound-on; harmless — segs are `-an` — but it
   may cost more).
- **Vocal-envelope verification used twice** this packet: the breath-gap check
  on a sync take (P-023 method, holds) and the new-mix clock check (law 1) —
  the envelope method is now a general verification instrument.

## Directors review (mid-packet)

- The **six-lens review workflow** ran mid-packet; **`DIRECTORS_NOTES.md`**
  committed (`acf8a2b`). Verdict: the story works, currently demo level →
  label-pitch after the must-fixes.
- **Section-clock error in ROADMAP VERIFIED** (beat-locked line map): CH1 is
  really 42.9s, FINAL only 16.2s, etc. — verified table at the bottom of
  DIRECTORS_NOTES.md. **PRE1 itself was unaffected** (70.3–89.8 correct).
  **Re-time is PENDING → the next paper packet (P-025).**

## Proof (frame-QC via vision subagent — sandbox eyes, law 0b)

- Every asset frame-QC'd before each spend: master candidate batches (4+4),
  walk scrub + trees derivation, takes, the raw wan (caught the turn-away +
  warped-hand failure pre-2K), the pinned retry, and the stitched outputs.
- **Final pass over first90.mp4 = SHIP**, PRE1 likeness **9/10** — after the
  stitch-drift fix was verified frame-exact (the corrected first90 re-queued
  for a full sandbox-eyes pass, `4476c34`/`79c10e4`).
- No software test suite (media packet) — exact counts are the QC passes
  above. Safety: no secrets committed; `song.mp3` (now the July Reverb mix)
  stays gitignored; no bare chest shipped (standing user hard rule held —
  wardrobe salvage was local rotate/crop, zero face passes).

## Reviewer verdict

- **SHIP** — the final frame-QC pass on first90 is the shipping judgment
  (accepted process deviation for media packets, precedent
  P-018/P-020/P-022/P-023; no formal qa/reviewer agents; Codex second-eyes not
  run — not applicable to the CI-relay media workflow).

## Commits

- **Base `2f791a8`** (the P-023 close commit; branch in sync with origin at
  open, 0 ahead / 0 behind). Range **`80f373f` → `057f739`** — **23 commits
  (15 authored relay + 8 CI render commits)**, plus this receipt/close commit
  on top. Key points: `80f373f` (PRE1 board), `f704335`/`b3347c0` (master
  candidates + gated picks + turn crop), `087d4bd`/`bb184b2` (measured cut
  points), `acf8a2b` (DIRECTORS_NOTES.md), `b59af45` (July Reverb slices, grid
  verified), `f94a86e` (failed turn take purged, pinned retry queued),
  `448d5f7` (frame-exact stitch fix), `057f739` (QC purge + HANDOFF refresh).
- **≤2-commit contract: acknowledged inapplicable** to the CI-relay media
  workflow (same recorded precedent as P-020/P-022/P-023).

## Credits

**1405.71 at open → 1371.75 at close** (~**34** spent vs the roadmap's
**150–200** estimate for PRE1 — user-gated picks, derivation over rerolls, and
the local rotate/crop salvage kept the spend low).

## Residue (mirrors HANDOFF.md — carried + new)

- **Training photos remain in PUBLIC git history** — purge offered, still
  unanswered (carried since P-018/P-019; re-raised at close). Needs explicit
  go (history rewrite).
- **3 bare-chest curb rolls still in the Higgsfield library**
  (`733152dc` / `8c9b80ef` / `85cb2d58`) — MCP cannot delete them; the user is
  to delete them in the Higgsfield UI (still unconfirmed).
- **Locked-intro jewelry flag:** thin chain + ring ~19–21s inside the
  user-LOCKED intro_v6 — flag to the user **before FINAL assembly**; fixing
  means reopening the lock. Record only.
- **kling `sound:"off"` must be explicit** — 2 P-024 takes rendered sound-on
  (harmless, segs are `-an`, but possibly costlier). Ops note, now law.
- **ROADMAP section clock is WRONG for the back half (VERIFIED)** — re-time
  pending in **P-025** (paper packet) along with the APPARITION LEGIBILITY
  STANDARD and the waterfront-photo plant (DIRECTORS_NOTES must-fix #1/#2 +
  STRUCTURAL).

## Open boundaries

- No merge / deploy / publish / secrets without explicit go (CI auto-push of
  render commits on this branch is the established relay mechanism, not a new
  mutation).
- Training-photo history purge: user-gated, unanswered.
- Locked material stays locked: hero `491e39d1`, chorus_v16, intro_v6.
- New Higgsfield generation only inside the next staged **media** packet with
  go (P-025 as staged is a PAPER packet — zero generation).

## Next

**PAPER PACKET P-025 — re-time ROADMAP to the verified section clock +
APPARITION LEGIBILITY STANDARD + waterfront-photo plant** (see
`when-it-rains/DIRECTORS_NOTES.md` must-fix #1/#2 and the STRUCTURAL section;
the verified clock table is its addendum). The **media packet formerly
numbered P-025** in the old roadmap numbering **shifts after** this paper
packet. Consequences the re-time must absorb: CH1 needs a plan for
102–132.7 (30.7s of chorus beyond the locked cut) and FINAL's mug-choice
ending must land in **16s, not 86**.
