# Receipt — P-026: first90 QUALITY PASS (resolution + face consistency)

**Status:** CLOSED — 1080p `first90.mp4` + `pre1_v1.mp4` re-delivered, both parts
(resolution + face consistency) verified by frame reads, verdict **SHIP**
(2026-07-03)
**Written by:** archivist.

## Scope

- **In:** User-directed QUALITY PASS after feedback that the delivered P-024/P-025
  first90 shots looked **"extremely like AI / degraded, not realistic."** TWO
  parts: (1) **RESOLUTION** — convert the entire render pipeline 1280×720 →
  1920×1080; (2) **FACE CONSISTENCY** — element-refit the remaining AI-looking
  sync faces to canonical. The cut SHAPE stays (same scenes, choreography,
  timings). Media packet under `when-it-rains/` (Topaz upscales, nano
  element-refits, wan re-syncs, CI-relay re-render, delivery — Higgsfield spend
  inside the packet go).
- **Out (explicit):** LOCKED intro_v6 / chorus_v16 / hero `491e39d1` (untouched);
  everything past 89.8; the AWNING face (kept at 1080p per explicit user choice —
  the one remaining non-canonical shot, flagged with an offer to refit); any
  merge / deploy / publish / secrets; the training-photo history purge (still
  user-gated, unanswered); the PAPER packet (ROADMAP re-time — renumbered P-026 →
  **P-027**, staged, not started).

## What shipped

- **1080p `first90.mp4`** (89.79s) — whole render pipeline converted to
  1920×1080; the "degraded body" (kitchen cabinet shot) is fixed, verified by the
  archivist-agent's own frame reads. Phone encode = 37MB crf24 1080p (delivers
  fine; the >50MB silent-fail law held — 37MB sent OK).
- **1080p `pre1_v1.mp4`** section cut, with the refit faces swapped in.
- Branch: **`claude/when-it-rains-handoff-l0u075`** (no trunk; green judged
  against the branch tip; CI relay branch-agnostic).

## Part 1 — RESOLUTION (1280×720 → 1920×1080)

- **Root cause:** the whole render pipeline was 1280×720 and the cut
  **DOWNSCALED even the high-res takes to 720p** — the real source of the
  "degraded" look.
- **All 4 scripts converted to 1920×1080** (`render_p024.sh`,
  `render_pre1.sh`, `render_v1_v2.sh`, `render_v2_v2.sh` — scale/pad, the PUNCH
  crop, and the curb `zoompan s=`).
- **Source-resolution findings:** the 4 sync takes + mugs were **natively 1440p**
  (the bytedance "2K" really was ~1440p — an EARLIER MISREAD called it 720p; kept
  native, downscaled in render). The **6 kling takes**
  (walk/door/rain/ghostwin/ghostpud/trees) were **~716p** → **Topaz-upscaled to
  1080p** (Topaz "prob-4" beats bytedance for real detail).

## Part 2 — FACE CONSISTENCY (element-refit remaining AI-looking faces)

- Method = nano **ELEMENT-REFIT with `wir-him` embedded** (P-025 law), same scene,
  face pulled to canonical, then re-sync. **User-gated** at each stage.
- **KITCHEN:** refit `b4afd0c6` → wan re-sync `68b54a2a` → Topaz-1080p
  `a608a76d` (into `render_v1_v2.sh` sync.mp4).
- **CURB:** needed a **tighter re-refit** — 1st round widened the pose + added
  pedestrians; **2nd round `ac4c68dc` held the composition** → wan `55a4ddff` →
  Topaz-1080p `a3b89b9b` (into `render_v2_v2.sh` curbsync.mp4).
- **AWNING:** kept at 1080p per explicit user choice — **still the older face**,
  the one remaining non-canonical shot; flagged to the user with an offer to
  refit if noticed.
- **walk + turn:** already canonical from P-025 — untouched.
- Both re-syncs **frame-QC'd by the archivist agent in the main loop** (NO
  subagents — per the user directive "stop using usage credits / use Opus 4.8"):
  canonical brown-eyed face confirmed, correct lip-sync including breath gaps,
  world-rushing intact.

## NEW LAWS (mirrored from the HANDOFF QUALITY PASS addendum)

1. **Render at 1080p — never downscale high-res takes to 720p.**
2. **Topaz (prob-4), not bytedance, for real detail on sub-1080p takes**
   (bytedance "2K" is ~1440p, not the fix for a 716p source).
3. **Nano element-refit REIMAGINES composition ~half the time** (widens pose /
   reframes / adds people) — lock the pose explicitly ("arms straight down at
   sides, do NOT widen/reframe, NO other people") and expect a reroll.
4. **Chat delivery:** a **37MB crf24 1080p encode delivers fine; >50MB fails
   silently** (delivery-size law, now confirmed at 1080p).

## Proof (frame reads — main-loop archivist eyes, no subagents this pass)

- Resolution fix verified by direct frame reads — the "degraded body" (kitchen
  cabinet shot) confirmed fixed at 1080p.
- Both re-syncs (kitchen, curb) frame-QC'd in the main loop: canonical brown-eyed
  face, correct lip-sync (breath gaps honored), world-rushing intact.
- 1080p `first90.mp4` (89.79s) delivered; phone encode 37MB, send OK.
- No software test suite (media packet) — exact counts are the frame-QC reads
  above. Safety: no secrets committed; `song.mp3` (July Reverb mix) stays
  gitignored; **no bare chest** (standing hard rule held); likeness law held
  (element-refit + Topaz, user-gated).

## Reviewer verdict

- **SHIP** — the archivist-agent frame reads on the 1080p first90 (resolution +
  both refit faces) are the shipping judgment. Accepted process deviation for
  media packets (precedent P-018/P-020/P-022/P-023/P-024/P-025); no formal
  qa/reviewer agents; **Codex second-eyes not run** (not applicable to the
  CI-relay media workflow). Note this pass ran **main-loop QC only, no
  subagents**, per the user's "stop using usage credits / use Opus 4.8"
  directive.

## Commits

- **Base `8ab4d8d`** (P-025 close). Range **`ed07272` → `fb26aad`** — **16
  commits (10 authored relay + 6 CI render commits)**, `git diff --stat
  8ab4d8d..fb26aad` = **10 files, +51 / −19** (the two big deltas are the
  `first90.mp4` 53MB→102MB and `pre1_v1.mp4` 18MB→35MB re-renders at 1080p).
  Working tree clean at close, **0 ahead / 0 behind origin**.
  Key points: `ed07272` (all 4 scripts → 1920×1080), `1175aac` (Topaz test
  outputs queued: mugs + walk), `0dcc0f2` (6 Topaz-1080p kling takes swapped in;
  1080p re-render triggered), `02204f8` (face-refit candidates
  kitchen/awning/curb queued), `20e01f2` (tighter curb re-refit queued),
  `a33a620` (kitchen + curb re-syncs queued for QC), `e0f8b15` (kitchen + curb
  1080p refit syncs into the cut; final face-consistency render), `fb26aad`
  (QC-frame purge + HANDOFF QUALITY PASS addendum).
- **≤2-commit contract: acknowledged inapplicable** — this pass ran **many small
  CI-relay + Topaz iterations** (not the ≤2-commit shape), the same accepted
  media-workflow precedent as P-020..P-025.

## Credits

- **1371.75 — unchanged across the whole pass** (same read before and after).
  The user's **enhanced Higgsfield account appears to be absorbing the generation
  cost** — flagged, **verify in the UI**. Treat 1371.75 as best-known; re-read at
  the next spend. (Continues the P-025 identical-balance oddity.)

## Residue (carried)

- **Training-photo history purge** — offered, still **unanswered** (carried since
  P-018/P-019). Needs explicit go (history rewrite = external mutation).
- **3 bare-chest curb rolls still in the Higgsfield library**
  (`733152dc` / `8c9b80ef` / `85cb2d58`) — user to delete in the Higgsfield UI
  (still unconfirmed).
- **Locked-intro jewelry flag:** thin chain + ring ~19–21s inside the
  user-LOCKED intro_v6 — decide **before FINAL assembly**. Record only.
- **NEW — AWNING non-canonical face (minor):** the awning sync keeps the older
  face at 1080p per the user's explicit choice; the only non-canonical face in
  the first90. Refit offered; leave unless the user raises it.
- **ROADMAP back-half clock still VERIFIED WRONG** — do NOT generate against it;
  the re-time is the **PAPER packet, renumbered P-026 → P-027** (below).
- New QC/render learnings (1080p-never-downscale, Topaz-not-bytedance,
  pose-lock-the-refit, 37MB-fine-50MB-silent) are canonized in the HANDOFF
  QUALITY PASS addendum — laws, not open items.

## Open boundaries

- No merge / deploy / publish / secrets without explicit go (CI auto-push of
  render commits on this branch is the established relay mechanism).
- Training-photo history purge: user-gated, unanswered.
- Locked material stays locked: hero `491e39d1`, chorus_v16, intro_v6.
- Enhanced-account credit accounting to be verified in the Higgsfield UI.
- New Higgsfield generation only inside a media packet with go — **the staged
  P-027 is a PAPER packet (zero generation)**.

## Next

**PAPER PACKET P-027 — re-time ROADMAP to the verified section clock +
APPARITION LEGIBILITY STANDARD + waterfront-photo plant** (per
`when-it-rains/DIRECTORS_NOTES.md` must-fix #1/#2 + STRUCTURAL; verified clock
table in its addendum — CH1 really 89.8–132.7 = 42.9s, FINAL only 16.2s). Zero
generation; required before any back-half generation. **Renumbered from the old
"P-026" paper staging** when this QUALITY PASS took the P-026 id (which had
itself been renumbered from the original "P-025" staging at the P-025 close).
